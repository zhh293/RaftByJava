# 分布式集群算法选型思维——从第一性原理到真实组件

> 学完 Raft 之后,回头看 ZooKeeper、Nacos、Sentinel、Kafka、TiDB 这些中间件的集群设计,会有一种强烈的困惑:它们的数据备份、一致性、容灾做法千奇百怪,Raft 似乎只是其中微不足道的一环。为什么有的用 Raft,有的用主从复制,有的用 Gossip?到底有没有一套通用的思维,让我看到任何一个组件、任何一个场景,都能判断出"它该用什么算法、为什么这么设计"?
>
> 这篇文档就是来回答这个问题的。它不教你某个具体算法的细节(那些另有专文),而是建立一套**自上而下的判断框架**:先讲清楚所有集群设计背后共通的几个根本权衡维度,再用这套维度去解剖每一个真实组件,最后收敛成一张可以反复查阅的"选型地图"。读完之后,你面对任何新组件,都能顺着这套逻辑推断出它的设计思路。

---

## 一、先破除一个幻觉:不存在"通用算法",只存在"通用权衡维度"

很多人学分布式的痛苦,源于一个错误的期待:希望找到一个"算法选择器",输入场景、输出算法。这个期待注定落空,因为分布式系统的真相是——**没有放之四海皆准的算法,只有放之四海皆准的权衡维度。**

Raft、Paxos、ZAB、主从复制、ISR、Gossip,这些不是互相竞争的"更优解",而是**站在不同权衡点上的不同选择**。一个组件选了 Raft,不是因为 Raft 更先进,而是因为它的场景恰好落在了 Raft 所擅长的权衡区间里。换个场景,Raft 可能就是最差的选择。

所以正确的学习路径不是去背"哪个组件用什么算法",而是去掌握**决定算法选择的那几个根本维度**。一旦你能对一个场景在这几个维度上准确定位,算法的选择几乎是自动浮现的。

这些维度只有少数几个,下面逐层展开。

---

## 二、第一性原理:任何集群设计都在回答三个根本问题

不管是 ZK、Nacos 还是 TiDB,它们的集群设计本质上都在回答同样的三个问题。这三个问题的答案,直接决定了它用什么算法。

### 问题一:这份数据,丢了要人命吗?

这是最根本的分水岭,它决定了你需要多强的一致性。

有一类数据是**系统的命门**:集群的成员列表、谁是主节点、分布式锁、关键配置、数据库的元信息。这类数据的特征是——**量小、读多写少、但绝对不能错、绝对不能脑裂出两个互相矛盾的"真相"**。配置错一个、服务发现指错一个节点、锁同时被两个人拿到,整个系统就崩了。这类数据必须用**强一致共识算法(Raft / Paxos / ZAB)**,哪怕牺牲一点可用性也在所不惜。

另一类数据是**业务洪流**:Kafka 里每秒几十万条的消息、数据库里 TB 级的业务记录、缓存里的热点 key。这类数据的特征是——**量大、写入频繁、追求极致吞吐**。如果对每一条都走"必须多数派确认才提交"的重共识流程,吞吐会被打到骨折。所以这类数据往往用**主从复制 + 可调一致性(如 Kafka 的 ISR、MySQL 的 binlog 复制)**,把一致性的强弱做成一个旋钮,交给业务自己拧。

还有一类数据是**状态扩散**:节点的存活探测、监控指标、集群拓扑的弱感知。这类数据**允许短暂不一致,只要最终能扩散到所有节点就行**。这类场景用 **Gossip(流言协议)**最划算——没有中心、没有选主、靠节点间随机两两交换信息,以极低的成本实现最终一致。

> **第一个判断动作:拿到一个场景,先问"这份数据错了/丢了,系统会不会崩"。答案越接近"会崩",就越往强一致共识靠;越接近"无所谓",就越往最终一致靠。**

### 问题二:谁来决定"谁是主"?——控制面与数据面的分离

这是整篇文档**最重要的一个洞察**,理解了它,一大半"千奇百怪"立刻就有了骨架。

很多人学 Raft 时形成了一个思维定势:**选主和数据复制是同一件事**。这是因为 Raft 故意把两者捆在一起做——同一套机制既选 Leader 又复制日志,这正是 Raft "易于理解"的代价和优雅之处。但在真实的大型组件里,**选主(控制面)和数据复制(数据面)往往是用两套完全不同的机制分开做的。**

- **控制面(Control Plane)**:管"谁是主、集群有哪些成员、配置是什么、元数据是什么"。数据量小,但要求绝对强一致。
- **数据面(Data Plane)**:管"海量业务数据怎么高效地复制到各个副本"。数据量大,要求高吞吐。

一旦你建立起"控制面归控制面、数据面归数据面"的拆分视角,就会发现绝大多数组件都遵循同一个套路:**控制面用强一致共识(Raft/ZAB/Paxos),数据面用主从复制或可调一致。**

最典型的例子就是 Kafka:
- **老版本 Kafka(2.8 之前)**:它自己不选主,把"谁是 Controller、哪个副本是分区 Leader"这种**控制面决策外包给了 ZooKeeper**(ZooKeeper 内部用 ZAB 这个强一致协议),自己只专注于做高吞吐的数据复制。这就是为什么老 Kafka 必须依赖 ZK。
- **新版本 Kafka(3.3+ 的 KRaft 模式)**:把控制面这块也用 Raft 的变体自己实现了(一个独立的 controller quorum 专门跑 Raft 来管元数据),从而踢掉了 ZK 依赖。但注意——**它的数据面(消息复制)依然不是 Raft,依然是 ISR 主从复制。** Raft 只用在了管理元数据的控制面上。

> **第二个判断动作:把一个组件拆成控制面和数据面两层,分别判断它们各自需要什么。你会发现控制面几乎清一色是强一致共识,数据面则五花八门。所谓"千奇百怪",差异主要在数据面。**

### 问题三:你的数据是"一份"还是"一片"?——全复制与分片

这个问题决定了共识算法的"作用范围"。

ZK、etcd、Nacos 的数据是**一份全量数据,每个节点都存完整副本**(全复制)。这种模式下 Raft/ZAB 完美适用,因为数据量小,全量复制毫无压力,整个集群跑一个共识组就够了。

但 TiDB 的数据是 TB、PB 级的,不可能让每个节点都存全量。所以 TiDB 的做法是**先把数据按范围切成很多分片(Region),再对每一个分片单独跑一个独立的 Raft 组**。这就是 **Multi-Raft**——成千上万个 Raft 小组并行工作,每个组只负责一小片数据、各自选自己的 Leader、各自复制自己的日志。此时 Raft 不再是"整个集群一个共识",而是"每片数据一个共识"。

> **第三个判断动作:看数据量级。数据小到可以全量复制,就单组共识;数据大到必须分片,就 Multi-Raft / 分片 + 每片独立复制。**

---

## 三、把三个问题展开成完整的权衡维度

上面三个根本问题,可以进一步展开成一张更细的判断表。当你拿不准一个场景该用什么算法时,逐项过一遍这张表,答案就清晰了。

| 判断维度 | 偏向 Raft/Paxos/ZAB(强一致共识) | 偏向 主从复制/ISR(高吞吐可调一致) | 偏向 Gossip(最终一致) |
|---|---|---|---|
| **数据重要性** | 元数据、配置、锁、选主(错了致命) | 业务消息、数据库记录、日志流 | 节点探活、监控、拓扑感知 |
| **数据量级** | 小(MB~GB,可全量复制) | 大(持续高速流入) | 任意,通常是小状态 |
| **读写比** | 读多写少 | 写多(高吞吐写入) | 读写都频繁但不要求强一致 |
| **一致性要求** | 线性一致,绝不脑裂 | 可调(业务通过参数自选) | 最终一致即可 |
| **写入延迟容忍** | 能接受多数派确认的往返延迟 | 要极低延迟,不愿等多数派 | 不关心单次延迟 |
| **是否需要选主** | 需要,且选主本身要强一致 | 通常需要,但选主可外包给共识组件 | 不需要中心节点 |
| **典型代表** | etcd / ZK(ZAB) / Nacos-CP / TiDB-PD | Kafka(ISR) / MySQL主从 / Redis主从 | Cassandra / Consul成员发现 / Redis Cluster 拓扑 |

这张表不是让你死记,而是提供一个**逐项打分的脚手架**。真实组件往往不是单选,而是"控制面落在第一列,数据面落在第二列"的组合——这正好印证了问题二的"控制面/数据面分离"思想。

---

## 四、贯穿一切的总开关:CAP 不是口号,是坐标轴

前面三个问题更多是"功能性"的判断。而所有这些判断之上,还笼罩着一个更高维度的总开关——**CAP 定理**。但请不要把 CAP 当成一句"三选二"的口号背下来,而要把它当成一根**坐标轴**来用。

在分布式系统里,网络分区(P)是不可避免的物理现实,你没有选择权——网线会断、机房会失联。所以真正的取舍永远是在 **C(一致性)和 A(可用性)** 之间:当分区真的发生时,你是**宁可拒绝服务也要保证数据一致(CP)**,还是**宁可返回稍旧的数据也要保证能用(AP)**?

- **配置中心、分布式锁、数据库元信息**:必须选 **CP**。宁可在选举期间短暂不可写,也绝不能给出两份矛盾的配置或让锁被两个人同时持有。etcd、ZooKeeper、Nacos 的配置模块、TiDB 的 PD 都是 CP。
- **服务发现的实例列表**:很多时候选 **AP** 更合理。哪怕返回的实例列表稍微过时一点(包含一个刚下线的节点,或漏掉一个刚上线的节点),也比"分区期间整个服务发现不可用、所有调用全部失败"要好。Eureka 是典型的 AP,Nacos 的服务发现模块也提供 AP 模式。

这里藏着一个极其关键、也极容易被忽略的洞察:

> **同一个组件的不同功能,CAP 取舍可以不一样。** 这正是理解 Nacos "为什么既有 CP 又有 AP 两种模式"的钥匙——因为它既做配置管理(要 CP),又做服务发现(可以 AP)。把一个组件看成"它整体是 CP 还是 AP"是粗糙的;真正精确的视角是"它的每一个功能模块各自站在 CAP 坐标轴的哪个点上"。

---

## 五、再细一层:一致性不是开关,是一条连续光谱

掌握了 CAP 之后,还要再破除一个二分法的幻觉:很多人以为一致性只有"强一致"和"最终一致"两档。实际上一致性是一条**连续光谱**,从强到弱依次是:

**线性一致(Linearizable) > 顺序一致(Sequential) > 因果一致(Causal) > 最终一致(Eventual)**

- **线性一致**:最强。任何读都能读到此前最新已提交的写,就像所有操作在一个全局时间线上顺序发生。Raft 通过 ReadIndex/Leader 读 + 多数派确认实现,成本最高。
- **顺序一致**:所有节点看到的操作顺序一致,但不要求和真实时间对齐。
- **因果一致**:有因果关系的操作保证顺序,无因果关系的可以乱序。
- **最终一致**:最弱。只保证"如果不再有新写入,所有副本最终会收敛到相同状态"。Gossip、Redis 异步主从都属于这一档。成本最低。

算法选择的本质,就是**在这条光谱上选一个点,然后用最低的成本去实现它**。Raft 实现的是线性一致(贵),Gossip 实现的是最终一致(极便宜),而 Kafka 的 ISR + `acks` 参数,本质上是给了你一个**可以在光谱上滑动的旋钮**:`acks=all` 滑向强一致,`acks=1`/`acks=0` 滑向弱一致。

> **第四个判断动作:不要问"要不要强一致",而要问"业务能容忍的最弱一致性是哪一档"。然后选一个刚好能达到那一档、且成本最低的算法。过度追求强一致是工程上常见的浪费。**

---

## 六、真实组件解剖:用上面的框架逐个验证

理论讲完了。下面把前面建立的框架——**三个根本问题 + CAP 坐标 + 一致性光谱 + 控制面/数据面分离**——套到每一个真实组件上,验证"真实世界确实是这么设计的"。

### 6.1 ZooKeeper:控制面强一致的鼻祖

**它是什么**:一个为分布式系统提供协调服务的组件——分布式锁、配置管理、命名服务、选主、集群成员管理。它存的全是**控制面数据**:小、关键、读多写少。

**用什么算法**:**ZAB(ZooKeeper Atomic Broadcast)**。ZAB 和 Raft 是近亲——都是 Leader-based 的强一致共识,都靠多数派提交,都通过选主避免写冲突。区别在于 ZAB 的设计更早(2008 年前后),它的"崩溃恢复 + 消息广播"两阶段模型和 Raft 的"选举 + 日志复制"在思想上高度同构。可以粗略地理解为:**ZAB 是 Raft 的前辈,解决的是同一类问题。**

**CAP 定位**:典型 **CP**。当发生分区、可用节点不足多数派时,ZK 宁可拒绝写入(甚至拒绝读,如果开启了同步读),也绝不返回不一致的数据。

**框架验证**:数据小且关键(问题一→强一致)、全量复制(问题三→单组共识)、本身就是给别人提供选主服务的(问题二→它就是控制面的化身)。完美落在强一致共识区间,所以用 ZAB。

### 6.2 Nacos:一个组件内部 CP 与 AP 并存的活教材

**它是什么**:阿里开源的服务发现 + 配置管理组件。注意它身兼两职,而这两职的数据特性完全不同。

**配置管理模块**:配置是关键数据,错了致命(问题一→强一致)。所以这部分用 **Raft 协议(基于 JRaft 实现)**,是 **CP** 的。

**服务发现模块**:服务实例列表是高频变动、且能容忍短暂过时的数据。如果为了强一致而在分区时让整个服务发现不可用,代价太大。所以 Nacos 的服务发现默认提供 **AP 模式**,底层用的是自研的 **Distro 协议**——一个类似 Gossip 思想的、最终一致的、去中心化的数据同步协议(每个节点负责一部分数据的写入,再异步扩散给其他节点)。同时 Nacos 也允许把服务发现切成 CP 模式(用 Raft),由用户按需选择。

**框架验证**:这正是第四节那个关键洞察的最佳例证——**同一个组件,配置模块走 CP(Raft),服务发现模块走 AP(Distro/最终一致)**,因为两类数据在"丢了要不要人命"这个根本问题上的答案完全不同。理解了控制面/数据面和 CAP 可以按模块拆分,Nacos 的"双协议"设计就一点都不奇怪了。

### 6.3 Sentinel(Redis 哨兵):只借用选举,放弃数据共识

> 注:这里的 Sentinel 指 Redis 的高可用组件"哨兵",和阿里的流控组件 Sentinel 同名但完全无关。

**它是什么**:Redis 的高可用方案。职责很单一——监控 Master 是否存活,挂了就从 Slave 里选一个提升为新 Master。它**不参与数据读写**,只是观察者和决策者。

**用什么算法**:Sentinel 之间选举出一个 Leader 来主导故障转移,这个选举**借用了 Raft 的选举思想**(任期、投票、多数派)。但请注意——**它只借用了 Raft 的"选举"这一半,彻底放弃了 Raft 的"数据复制"那一半。**

**数据面为什么不用 Raft**:Redis 的核心诉求是**极致低延迟**(它是内存缓存,追求微秒级响应)。Raft 的"每次写入等多数派确认"会带来几毫秒的网络往返,对 Redis 是不可接受的性能打击。所以 Redis 的数据复制是**异步主从**——Master 写完内存立刻返回客户端成功,再异步推给 Slave。这意味着 Master 宕机可能丢失最后几条还没来得及复制的数据,但 Redis 认为"缓存偶尔丢一两条"远比"每次写入慢 10 倍"划算。

**框架验证**:这是"控制面/数据面分离"的又一个经典案例,而且分离得非常极端——**控制面(选主)用强一致的 Raft 思想,数据面(复制)用最弱的异步最终一致**。因为 Redis 在 CAP 坐标上整体偏向 AP(问题一的答案是"缓存丢点数据无所谓"),所以数据面果断放弃强一致换取低延迟,只在"选主"这个真正不能脑裂的环节保留了共识思想。

### 6.4 Kafka:控制面与数据面分离的教科书

**它是什么**:高吞吐的分布式消息队列。核心诉求是**吞吐**——每秒几十万到上百万条消息。

**数据面(消息复制)**:绝不可能用 Raft。Kafka 用的是 **ISR(In-Sync Replicas,同步副本集合)+ 主从复制**。每个分区有一个 Leader 副本和若干 Follower 副本,Follower 主动从 Leader 拉取消息。Leader 维护一个 ISR 集合——那些"跟得够紧"的 Follower。一条消息是否算"提交",取决于客户端设置的 `acks` 参数:
- `acks=0`:发出去就不管,最快,可能丢。
- `acks=1`:Leader 写入本地就返回,Leader 宕机可能丢。
- `acks=all`:等 ISR 里所有副本都确认才返回,最安全,最慢。

这就是把一致性做成了**旋钮**——业务自己在一致性光谱上滑动选点。这是数据面追求高吞吐的必然选择。

**控制面(元数据管理)**:谁是 Controller、每个分区的 Leader 是谁、ISR 集合是什么——这些是**关键的控制面数据,必须强一致**。
- **老版本(2.8 前)**:控制面外包给 **ZooKeeper(ZAB)**,Kafka 自己不做控制面共识。
- **新版本(3.3+ KRaft 模式)**:用一个独立的 **controller quorum 跑 Raft 的事件驱动变体**来管元数据,踢掉了 ZK。

**框架验证**:Kafka 是"控制面/数据面分离"最干净的教科书——**控制面用强一致共识(ZAB 或 KRaft),数据面用可调一致的 ISR 主从复制**。注意即便 KRaft 把控制面换成了 Raft,数据面的消息复制**依然没用 Raft**,依然是 ISR。这彻底说明:Raft 在 Kafka 里只服务于"小而关键"的元数据,绝不碰"大而高吞吐"的消息流。

### 6.5 TiDB:Multi-Raft + 控制面 Raft 的集大成者

**它是什么**:分布式 NewSQL 数据库,数据量级是 TB、PB 级。

**控制面(PD,Placement Driver)**:PD 负责管理集群元信息——每个数据分片(Region)在哪些节点上、谁是 Leader、负载怎么调度。这是关键的控制面数据,PD 自身用 **etcd(Raft)** 来保证强一致和高可用。

**数据面(TiKV 存储层)**:数据量太大,不可能全量复制。TiDB 把数据按 key 范围切成无数个 Region(默认每个约 96MB),**每个 Region 单独跑一个独立的 Raft 组**(Multi-Raft)。成千上万个 Raft 组并行工作,各自选 Leader、各自复制日志、各自达成共识。

**框架验证**:TiDB 同时验证了三个判断动作——
- 问题一(数据库数据当然要强一致)→ 数据面也用了 Raft(而不是像 Kafka 那样用 ISR),因为数据库对一致性的要求远高于消息队列;
- 问题二(控制面/数据面分离)→ PD 控制面用 etcd/Raft,数据面用 TiKV 的 Multi-Raft,两套独立;
- 问题三(数据是"一片"不是"一份")→ 数据量巨大必须分片,所以是 Multi-Raft 而非单组 Raft。

TiDB 是把前面所有框架揉在一起的集大成者:**它的数据面之所以也敢用 Raft(不像 Kafka 用 ISR),是因为数据库的一致性诉求压倒了吞吐诉求;而它之所以用 Multi-Raft 而非单 Raft,是因为数据量大到必须分片。** 每一个设计决策都能在框架里找到对应的判断依据。

---

## 七、收敛成一张决策流程图

把前面所有的判断动作串成一条可执行的决策路径:

```
拿到一个集群设计场景 / 一个新组件
        │
        ▼
【第一步】拆分:这是控制面还是数据面?
        │
        ├──→ 控制面(选主/成员/配置/元数据)
        │         │
        │         ▼
        │   数据量小、关键、不能脑裂 ──→ 强一致共识(Raft / ZAB / Paxos)
        │
        └──→ 数据面(业务数据的复制)
                  │
                  ▼
        【第二步】数据量级有多大?
                  │
                  ├──→ 小(可全量复制) ──→ 单组共识(Raft/ZAB)
                  │
                  └──→ 大(必须分片)
                            │
                            ▼
              【第三步】一致性要求有多强?
                            │
                            ├──→ 必须线性一致(如数据库) ──→ Multi-Raft(每片一个 Raft 组)
                            │
                            ├──→ 可调一致(如消息队列) ──→ ISR 主从复制 + acks 旋钮
                            │
                            └──→ 最终一致即可(如缓存) ──→ 异步主从复制 / Gossip
```

**用这张图验证前面的组件:**

- ZooKeeper:控制面 → 小且关键 → ZAB。完毕。
- Nacos 配置:控制面 → 小且关键 → Raft。Nacos 服务发现:数据面 → 可容忍最终一致 → Distro(类 Gossip)。
- Redis Sentinel:控制面(选主) → Raft 选举。数据面(缓存) → 最终一致 → 异步主从。
- Kafka:控制面 → KRaft/ZAB。数据面 → 大 → 可调一致 → ISR。
- TiDB:控制面(PD) → etcd/Raft。数据面 → 大 → 必须线性一致 → Multi-Raft。

每一个组件都能在这张图里找到自己的位置,没有例外。

---

## 八、几个容易混淆的问题

### 8.1 Raft 和 ZAB 到底有什么区别?

从工程效果上看,几乎没有本质区别——都是 Leader-based 的强一致共识,都靠多数派提交,都通过选主避免写冲突。区别主要在于:

- **历史先后**:ZAB 更早(2008),Raft 更晚(2014),Raft 的设计目标之一就是"比 Paxos 和 ZAB 更容易理解"。
- **恢复机制**:ZAB 在 Leader 崩溃后有一个显式的"发现(Discovery)→ 同步(Synchronization)→ 广播(Broadcast)"三阶段恢复流程;Raft 把恢复融入了正常的选举和日志同步流程中,没有单独的恢复阶段。
- **日志处理**:Raft 允许 Leader 覆盖 Follower 的冲突日志;ZAB 在恢复阶段会确保新 Leader 拥有最完整的事务历史,然后同步给所有 Follower。

但从"选型"的角度看,你可以把它们视为同一类——**都是解决"小数据强一致"问题的共识算法**。选 ZAB 还是 Raft 更多是历史原因(ZK 诞生时 Raft 还没发明),而不是场景差异。

### 8.2 ISR 和 Raft 的多数派有什么本质区别?

这是一个非常好的问题,因为表面上看它们都是"一组副本确认后才提交"。但本质区别在于:

**Raft 的多数派是固定的数学约束**:5 个节点,多数派永远是 3。不管谁快谁慢,必须等到 3 个确认才提交。慢节点会拖慢整体延迟。

**Kafka 的 ISR 是动态的集合**:ISR 里只包含"跟得上"的副本。如果一个 Follower 太慢,它会被踢出 ISR;等它追上来再加回去。这意味着 Kafka 的"确认集合"是弹性的——慢节点不会拖慢提交,只是被暂时排除在外。代价是:如果 ISR 缩小到只剩 Leader 一个,`acks=all` 实际上退化成了 `acks=1`,此时 Leader 宕机就会丢数据。

**本质差异**:Raft 用固定多数派换取了**绝对的安全性保证**(任何情况下已提交数据不丢);Kafka 用动态 ISR 换取了**更高的吞吐和更低的延迟**(慢节点不拖后腿),但代价是在极端情况下安全性会降级。这正是"数据库"和"消息队列"在一致性光谱上的不同站位。

### 8.3 Gossip 到底适合什么场景?

Gossip 的核心特征是:**没有中心节点、没有选主、靠节点间随机两两交换信息来扩散状态。** 它的优势是极其简单、极其抗毁(任何节点挂了都不影响协议运行)、扩展性极好(节点数翻倍,收敛时间只增加 O(logN))。

但它的代价也很明确:**无法保证任何时刻所有节点看到的状态一致,只能保证"最终"一致。** 而且"最终"的时间窗口不确定——可能是几百毫秒,也可能是几秒。

所以 Gossip 适合的场景是:

- **集群成员探活**(Consul 的成员发现、Redis Cluster 的节点存活检测):节点是否存活这个信息,晚几百毫秒知道无所谓。
- **监控指标扩散**:各节点的 CPU、内存使用率,不需要全局强一致。
- **去中心化的拓扑感知**:每个节点知道集群大致长什么样就行,不需要精确到毫秒级同步。

**绝对不适合 Gossip 的场景**:选主、分布式锁、配置管理——任何需要"全局只有一个真相"的场景。

### 8.4 为什么 TiDB 的数据面敢用 Raft,而 Kafka 不敢?

因为它们对一致性的诉求等级不同。

TiDB 是**数据库**。数据库对用户的承诺是:事务提交了就绝对不丢,读到的一定是最新的。这个承诺不允许任何"旋钮"——你不能跟用户说"你可以选择偶尔丢一条银行转账记录来换取更高吞吐"。所以 TiDB 的数据面必须用 Raft 这种"固定多数派、绝对不丢"的机制,哪怕牺牲一些吞吐。

Kafka 是**消息队列**。消息队列的典型场景是日志收集、事件流、异步解耦。这些场景下,"偶尔丢一两条日志"的代价远远小于"吞吐从 100 万/秒降到 10 万/秒"的代价。所以 Kafka 选择了 ISR 这种"可调一致、高吞吐"的机制,把选择权交给业务。

**本质上是业务场景对"数据丢失"的容忍度不同,决定了数据面用不用 Raft。** 这正是问题一("这份数据丢了要人命吗")的直接体现。

### 8.5 框架推导出"需要强一致",但组件本身没有内嵌 Raft——以 Sentinel(阿里流控)为例

这是一个极好的思维实验,它揭示了框架中一个隐含的工程决策层。

**场景**:假设你要设计 Sentinel(阿里的流控组件)的集群模式。Sentinel 存的核心数据是**流控规则**——限流阈值、降级策略、热点参数规则等。

**用框架推导一遍:**

**第一步:拆。** 流控规则是什么性质的数据?量很小(所有规则加起来可能只有 KB 级),读多写少(规则配置好之后主要是各节点读取并执行),而且**绝对不能在不同节点上出现两套互相矛盾的规则**(否则同一个接口在不同节点上限流行为不一致,等于限流形同虚设)。这完全符合**控制面数据**的特征——小、关键、不能脑裂。

不需要独立的数据面——Sentinel 不处理海量业务数据的复制,它只是存规则并执行。

**第二步:问。**

- 问题一:规则丢了/错了,系统会不会崩?→ 不会"崩",但流控失效可能导致雪崩,后果严重。→ **需要强一致**。
- 问题二:需要选主吗?→ 所有节点需要看到同样的规则,规则变更需要同步到所有节点。要么有一个主节点负责分发(需要选主),要么所有节点从同一个强一致源拉取。→ **需要某种形式的强一致共识**。
- 问题三:数据量级?→ 极小,每个节点完全可以存全量副本。→ **单组共识即可**。

**第三步:定位。** 一致性光谱上→ 需要线性一致(所有节点在任何时刻看到的规则都一样)。

**第四步:验证 CAP。** 当分区发生时,宁可暂时用旧规则(短暂不更新),也不能让不同节点执行不同规则。→ **CP**。

**推导结论:应该用 Raft 或等价的强一致共识,跑一个多节点集群,每个节点存全量规则副本。** 这和 etcd、ZooKeeper 的定位几乎一模一样——典型的"小数据强一致控制面"场景。

**但是——真实的 Sentinel 并没有自己内嵌 Raft。**

Sentinel 的集群模式实际上比较简陋:规则的持久化和同步依赖**外部配置中心**(Nacos、ZK、Apollo 等)来做推送,或者各节点独立从数据源拉取。Sentinel 自己节点之间并没有跑一个内置的共识协议来同步规则。

**这是否意味着框架推导错了?不是。** 框架推导出的结论——"这里需要强一致共识"——**完全正确**。只是 Sentinel 的设计者在"怎么实现这个强一致"上选择了一条不同的路径:**外包而非自建**。

这揭示了框架中一个隐含的、但极其重要的工程决策层:

> 当你通过框架推导出"这里需要强一致共识"之后,还有一个**后续的工程问题**需要回答:**是自己内嵌一个 Raft(Build),还是把这个需求外包给已有的共识基础设施(Buy)?**

这个"build vs buy"的选择取决于:

- **组件的定位和复杂度预算**:Sentinel 定位为一个轻量的流控 SDK/Sidecar,它不想变成一个重量级的分布式系统。如果为了规则同步而内嵌一个 Raft 实现,组件复杂度会陡然上升,和它"轻量嵌入"的定位矛盾。
- **生态中是否已有现成的共识基础设施**:在阿里/美团的技术栈里,Nacos、ZK 这些强一致组件已经是基础设施标配。既然它们已经解决了"小数据强一致"的问题,Sentinel 直接把规则存进去,靠它们来保证一致性,自己只做一个无状态的"规则执行器"——这是更务实的工程选择。

**这和老版本 Kafka 的思路完全一致。** 老 Kafka 面对的也是同样的判断链路:控制面需要强一致→但 Kafka 自己不想实现共识→把控制面**外包给 ZooKeeper**。直到 Kafka 发展到一定规模,外部依赖带来的运维负担超过了自建的成本,才在 3.3+ 版本用 KRaft 把控制面收回来自己做。Sentinel 只是还停留在"外包阶段",因为它的复杂度和独立性还没有达到需要自建共识的临界点。

**完善后的决策流程(补充一层):**

```
框架推导结论:这里需要强一致共识
        │
        ▼
  后续工程决策:Build or Buy?
        │
        ├──→ 组件定位重量级 + 需要独立部署 + 不想依赖外部
        │         → 自建 Raft(如 TiDB-PD、KRaft、ORC)
        │
        └──→ 组件定位轻量 + 生态中已有共识基础设施
                  → 外包给 Nacos/ZK/etcd(如 Sentinel、老版本 Kafka)
```

**这不削弱框架的有效性,恰恰相反,它补全了框架的最后一环。** 框架的职责是告诉你"这里需要什么级别的一致性",这一步推导 Sentinel 和 etcd 的答案是一样的——都是"需要强一致"。至于这个强一致是自己实现还是借用别人的,那是决策流程图**下游的实现选择**,不影响上游的判断正确性。就像你去医院,医生诊断出"你需要做手术"(框架的判断),至于是在本院做还是转院(build vs buy),那是另一个层次的决策了——但不改变"需要手术"这个诊断结论。

---

## 九、更多组件的快速定位(速查表)

| 组件 | 控制面算法 | 数据面算法 | CAP 偏向 | 核心诉求 |
|---|---|---|---|---|
| **ZooKeeper** | ZAB(自身就是控制面) | 无独立数据面 | CP | 强一致协调 |
| **etcd** | Raft(自身就是控制面) | 无独立数据面 | CP | 强一致 KV |
| **Nacos 配置** | Raft(JRaft) | 无独立数据面 | CP | 配置不能错 |
| **Nacos 服务发现** | — | Distro(类 Gossip) | AP | 高可用优先 |
| **Consul** | Raft(控制面) | Gossip(成员探活) | CP(KV) / AP(探活) | 服务网格 |
| **Redis Sentinel** | Raft 选举(简化版) | 异步主从 | AP | 极致低延迟 |
| **Redis Cluster** | Gossip(拓扑) + Raft 选举(故障转移) | 异步主从 | AP | 极致低延迟 + 水平扩展 |
| **Kafka(老版本)** | ZooKeeper(ZAB) | ISR 主从复制 | 可调(acks) | 高吞吐 |
| **Kafka(KRaft)** | KRaft(Raft 变体) | ISR 主从复制 | 可调(acks) | 高吞吐 |
| **TiDB/TiKV** | PD(etcd/Raft) | Multi-Raft(每 Region 一组) | CP | 强一致数据库 |
| **CockroachDB** | 内嵌 Raft | Multi-Raft | CP | 强一致数据库 |
| **MySQL 主从** | 无(人工/MHA/Orchestrator) | binlog 异步/半同步复制 | AP(异步) / 偏 CP(半同步) | 传统关系库 |
| **MySQL Group Replication** | Paxos 变体(组通信) | Paxos 变体(同一套) | CP | 强一致多主 |
| **Cassandra** | Gossip(无中心) | Quorum 读写(可调 W+R>N) | 可调(AP 或 CP) | 高可用写入 |
| **Elasticsearch** | Zen/选主(类 Bully) | 主从同步复制 | 偏 CP(写) | 搜索引擎 |

---

## 十、总结:一套可以反复使用的思维框架

回到最初的困惑:"这些组件做法千奇百怪,怎么用一套通用逻辑来判断?"

答案是四步:

**第一步:拆。** 把组件拆成控制面和数据面。控制面几乎清一色是强一致共识,不用纠结。真正需要判断的是数据面。

**第二步:问。** 对数据面问三个问题——数据丢了要人命吗?数据量大到必须分片吗?业务能容忍的最弱一致性是哪一档?

**第三步:定位。** 根据答案在一致性光谱上找到你的位置,然后选成本最低的算法去实现那个位置。

**第四步:验证。** 用 CAP 做最终校验——当分区发生时,你选的方案是牺牲 C 还是牺牲 A?这个牺牲对你的业务可以接受吗?

掌握了这四步,你面对任何新组件、任何新场景,都不会再觉得"千奇百怪"——因为它们都只是在同一个权衡空间里选了不同的点而已。差异不是混乱,而是不同业务诉求的必然结果。

---

> 最后一句话:分布式系统没有银弹,只有取舍。而取舍的智慧,不在于记住每个组件的做法,而在于理解它**为什么**这么做。当你能对任何一个设计决策说出"因为它的场景在这几个维度上是这样的,所以它必然会选这个算法"的时候,你就真正掌握了分布式集群设计的思维方式。

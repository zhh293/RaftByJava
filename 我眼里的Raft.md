# 我眼里的 Raft

> 这是我在学习 Raft 算法、阅读研究提纲、对照代码实现后，用自己的话梳理出来的理解。不追求学术严谨，但追求把每个环节想明白。

---

## 一、为什么要选主

分布式系统里多个节点都能接收写请求的话，冲突是必然的——两个节点同时收到对同一个 key 的写操作，到底听谁的？这个问题没有好的分布式解法，最简单的办法就是不让这个问题出现：所有写操作都由一个节点统一处理，其他节点只负责同步。这个唯一能处理写操作的节点就是 Leader。

所以选主这件事的根本目的就是**避免写冲突**，毋庸置疑。

---

## 二、选主的完整过程

### 初始状态

所有节点刚启动的时候都是 Follower，每个节点启动一个选举定时器，超时时间在一个区间内随机取值（比如 300ms 到 500ms 之间）。随机是关键——如果所有节点的超时时间一样，大家同时发起选举，谁都拿不到多数票，就僵住了。

### 发起选举

某个节点的选举定时器先到时间，它就准备竞选了。但这里有一个前置步骤：**Pre-Vote（预投票）**。

为什么需要 Pre-Vote？想象一个场景：集群正常运行着，某个节点因为网络分区被隔离了。它收不到 Leader 的心跳，就会不断超时、不断发起选举、不断自增 term。等网络恢复的时候，它带着一个异常高的 term 回来了，这个高 term 会迫使正常的 Leader 退位（因为 Raft 的规则是看到更高的 term 就退位），触发一次完全没必要的全集群重新选举。在选举期间，写请求没法处理，平白浪费了可用性。

Pre-Vote 就是解决这个问题的。节点不直接发起真正的选举，而是先"试探性地问一圈"：我打算竞选了，你们觉得我有戏吗？只有多数派回复说"你可以试试"，它才真正自增 term 发起选举。这样被隔离的节点发出的 Pre-Vote 请求根本得不到多数派响应（因为它跟大分区通信不了），也就永远不会真正自增 term，网络恢复后不会捣乱。

**那已经同步到正常节点上的数据会不会因为这次无意义的选举而丢失？** 不会。即使没有 Pre-Vote 保护，真的触发了重新选举，选举规则保证了当选的节点必须拥有最新的已提交日志。那个分区回来的节点 term 虽然高，但日志很旧，投票时其他节点一比较日志新旧就会拒绝给它投票，它当不了 Leader。所以 Pre-Vote 要防止的不是数据丢失，而是一次无意义的服务中断。

### Pre-Vote 通过后的真正选举

Pre-Vote 拿到多数派认可之后，节点才开始真正的选举：

1. **term++**：把自己的任期加一，进入新任期
2. **给自己投一票**
3. **角色切换为 Candidate**
4. **向所有其他节点广播投票请求（RequestVote）**，请求里携带自己的 term、最后一条日志的 term 和 index

同时启动一个**竞选超时定时器**，这个和选举超时定时器是分开的。选举超时是 Follower 等 Leader 心跳用的，竞选超时是 Candidate 等投票结果用的。如果竞选超时了还没凑齐多数票，说明这轮没戏，退回 Follower 重新等选举超时。

### 其他节点收到投票请求后怎么判断

这是整个选举机制里最核心的判断逻辑。收到 RequestVote 请求后，节点**按顺序**做以下检查——顺序很重要，前面的不通过，后面的根本不看：

**第一步：比 term。** 如果 Candidate 的 term 比自己的小，直接拒绝。term 小意味着这个 Candidate 的信息是过时的，它可能中间宕机过、被网络分区隔离过，总之它不了解集群的最新状况。不过如果 Candidate 的 term 比自己的大，节点要先把自己的 term 更新到 Candidate 的值，然后退回 Follower 状态（不管自己之前是什么角色），再继续后面的判断。

**第二步：查投票记录。** 在同一个 term 里，每个节点只能投一票。如果自己已经投过票了（votedFor 不为空），并且投的不是这个 Candidate，直接拒绝。这条规则是"一个任期只能选出一个 Leader"的基石。

**第三步：比日志新旧。** 这一步的判断规则稍微复杂一点，也是最容易搞混的地方。

首先要特别注意：**这里比较的 lastLogTerm 和前面第一步比较的 term 是完全不同的两个东西。** 第一步的 term 是 Candidate 的"当前任期号"，代表它经历了多少轮选举；而 lastLogTerm 是它最后一条日志所属的任期，代表它实际参与了多少轮有效的日志写入。这两者可以差很远——比如一个被网络隔离的节点不断超时选举，把自己的 term 刷到了 100，但因为隔离期间根本收不到任何日志，它的 lastLogTerm 可能还停留在 3。所以"term 高"不等于"日志新"。

比较规则是这样的：

- 先比最后一条日志的 **lastLogTerm**。Candidate 的 lastLogTerm 比自己的大，说明 Candidate 实际参与过更新任期的日志写入，日志更新，通过。
- 如果 lastLogTerm 相同，再比最后一条日志的 **lastLogIndex**。Candidate 的 lastLogIndex 不小于自己的，说明 Candidate 的日志至少和自己一样长，通过。
- 其他情况都是"Candidate 的日志比自己旧"，拒绝。

注意这里是**先比 lastLogTerm 再比 lastLogIndex**。为什么不直接只比 index 呢？因为 index 大不代表日志就更"正确"——一个节点可能在一个旧 term 里积累了很多未提交的日志（比如它曾经是 Leader 但还没来得及提交就宕机了），index 很大但都是无效的。lastLogTerm 才是更可靠的新旧指标。

回到前面那个 Pre-Vote 的例子：被隔离的节点即使没有 Pre-Vote 保护，带着 term=100 回来发起选举，第一步（比请求 term）和第二步（查投票记录）确实都能过，但到第三步就卡住了——它的 lastLogTerm 可能只有 3，而正常节点跟着正常 Leader 工作，lastLogTerm 已经到了 5、6、7，3 比 5 小，直接拒绝。它凑不齐多数票，当不了 Leader。所以 Pre-Vote 防的不是数据安全问题，而是那个高 term 会逼迫正常 Leader 在第一步就退位（看到更高 term 必须 stepDown），导致一段无谓的服务中断。

只有上面三步全部通过，节点才会投赞同票，同时记录下自己投给了谁（持久化到磁盘，防止重启后重复投票），并重置自己的选举定时器（既然投了票，说明有人在竞选了，自己就不急着超时了）。

**这里有一个我之前搞混的问题：怎么保证一个任期只有一个 Candidate 在选举？** 实际上根本不保证这一点。同一个 term 里完全可以有多个节点同时变成 Candidate 发起选举，这就是所谓的"选票瓜分"。安全性靠的不是"只有一个 Candidate"，而是"一票只投一人"加上"需要多数派同意"。两个多数派之间必然有交集（这是数学事实），所以不可能有两个 Candidate 同时拿到多数票。选票瓜分的结果就是大家都凑不齐多数票，这一轮作废，等竞选超时后退回 Follower，靠随机的选举超时让下一轮大概率只有一个节点先超时。

### Candidate 等待结果

Candidate 广播完投票请求后，等待各节点的回复，统计赞同票和拒绝票：

- **赞同票达到多数派**（超过半数）：竞选成功，调用 `becomeLeader()`，切换为 Leader。
- **拒绝票达到多数派**：不用再等了，不可能赢了，直接退回 Follower，重置选举定时器。这比傻等竞选超时要高效。
- **竞选超时**：没有明确结果（比如一些节点没回复），退回 Follower，重新来。

### 成为 Leader 后的第一件事

新 Leader 上任后会立即追加一条**空日志（no-op）**并广播给所有 Follower。这不是可有可无的，而是安全性必需的。原因比较微妙：Raft 规定 Leader 只能通过提交**当前任期**的日志来推进 commitIndex，不能直接提交旧任期的日志。新 Leader 上来如果不写一条自己任期的日志，就没法推进提交，那些旧任期遗留的已复制但未提交的日志就卡住了。no-op 就是来"占个位"的，让新任期有东西可以提交，旧日志也跟着一起被间接提交了。

---

## 三、Leader 宕机了怎么办

首先是某个 Follower 感知到 Leader 宕机了——具体说就是选举定时器超时了，一直没收到 Leader 的心跳。然后走上面那套完整的选举流程（Pre-Vote → 真正选举 → 上位）。

在这段没有 Leader 的时间里，**读请求要分情况**：如果客户端要的是最终一致性读（不要求最新数据），那读请求打到任何一个 Follower 都能正常返回，因为 Follower 的状态机虽然可能稍微落后，但数据是有效的。如果要的是线性一致性读，那就没办法了，必须等新 Leader 选出来。**写请求**则一律要返回错误或者告诉客户端"正在选举，请稍后重试"。

如果之后宕机的 Leader 恢复了（重启或者网络分区恢复），它也不会继续当 Leader。因为 Leader 通过心跳跟其他节点保持联系，心跳里带着 term 信息。新 Leader 上任后 term 已经更高了，旧 Leader 一旦收到任何包含更高 term 的消息（可能是新 Leader 的心跳，也可能是某个节点的投票请求），就会发现自己的 term 落后了，立即退位成为 Follower。这个退位检查遍布代码的各个消息处理入口——收到 AppendEntries、RequestVote、PreVote 响应时都会检查 term，只要发现更高的 term 就 stepDown，没有例外。

---

## 四、日志的读取

读请求分两种模式。

### 线性一致性读（Linearizable Read）

要求读到的一定是最新已提交的数据，不能有任何过时。这种读请求**必须由 Leader 处理**。

如果请求打到了一个 Follower，Follower 会返回一个重定向，告诉客户端 Leader 是谁，让客户端自己去找 Leader。

但 Leader 也不能直接读了就返回——它有可能已经被网络分区隔离了，自己还不知道集群已经选出了新 Leader（"不知大清已亡"）。如果直接返回，可能返回的是过时数据。所以 Leader 在处理线性一致性读之前，会先做一个**ReadIndex 确认**：记下当前的 commitIndex 作为"我要保证至少读到这个版本"的水位，然后向所有 Follower 发一轮心跳。只有多数派回复了心跳确认（证明"大清还在"），并且状态机已经把 commitIndex 对应的日志都应用完了，才从状态机读取数据返回给客户端。

### 最终一致性读（Eventual Consistency Read）

不要求一定是最新数据，允许稍微落后。这种读请求**任意节点都可以服务**，直接从本地状态机读就行。

但这里有一个问题：**客户端怎么避免"读回退"？** 比如客户端第一次读到了一个比较新的值（来自一个追赶得快的节点），第二次请求打到了一个落后的节点，反而读到了旧值。

解决方案是**appliedIndex 水位追踪**。工作原理是这样的：每次读响应里会带上当前节点的 `appliedIndex`（状态机已经应用到了哪一条日志）。客户端把这个值记下来。下一次读的时候把上次拿到的 `appliedIndex` 作为 `minAppliedIndex` 带在请求里，意思是"我至少要读到这个版本的数据"。服务节点收到后，看一下自己的 `lastApplied` 有没有达到这个要求——如果达到了，正常返回数据和更新后的 `appliedIndex`；如果没达到，返回一个"我这里数据还不够新，你换个节点试试"的拒绝响应。这样客户端读到的数据就只会越来越新，不会回退，实现了**单调读**的保证。

当然，这个前提是客户端愿意配合维护这个水位。如果客户端不带 `minAppliedIndex`，那就是纯粹的最终一致性，可能读到旧数据，但不会读到错误数据。

---

## 五、日志的写入

写请求必须由 Leader 处理，这是 Raft 的基本规则。如果客户端把写请求打到了一个 Follower，Follower 不会拒绝客户端，而是会**自动转发给 Leader**。具体实现是 Follower 给这次转发生成一个标识（forwardingId），记住原始客户端的连接通道，然后把请求发给 Leader。Leader 处理完之后响应里带着同样的 forwardingId 回来，Follower 再通过这个标识找到原始客户端的连接通道，把结果中继回去。对客户端来说是完全透明的，不需要知道集群的拓扑结构。如果 Follower 连 Leader 是谁都不知道（正在选举），那就只能返回错误让客户端重试了。

### 写入的两阶段提交

Leader 收到写请求后的处理流程：

**第一阶段（Propose 提议）**：Leader 把命令写入自己的预写日志（WAL），给它分配当前 term 和递增的 index，然后把这条日志通过 AppendEntries RPC 广播给所有 Follower。注意这里 Leader 不会立即响应客户端——它把这个"还没完成"的写请求挂在一个 pendingWrites 队列里等着。

**第二阶段（Commit 提交）**：Follower 收到日志后会校验（下面详细说），校验通过就追加到自己的日志里并返回成功。Leader 收集到**多数派确认**后，推进 commitIndex，然后按顺序把日志应用到状态机，再从 pendingWrites 里找到对应的客户端请求，返回成功响应。

如果 Leader 在日志提交前宕机了，pendingWrites 里的所有等待中的客户端请求都会收到一个"Leader 退位了"的错误，客户端需要自行重试。新选出的 Leader 会继续完成那些已经被多数派复制但还没提交的日志。

### Follower 收到日志同步请求后的校验

这里涉及到 term 和 index 的比较，但**跟选举投票时的比较逻辑完全不同**，不要搞混了。

AppendEntries 请求里带着 Leader 的 term、prevLogIndex（"我要从这个位置之后开始追加"）、prevLogTerm（"这个位置上的日志应该是这个 term 的"）、以及要追加的日志条目。

Follower 的校验顺序：

**第一步：比请求中的 term。** 如果 Leader 的 term 比自己的小，说明这个 Leader 是过时的，直接拒绝。并且在响应里带上自己的 term，Leader 收到这个响应后会发现 term 落后了，立刻退位。

**第二步：认可 Leader 权威。** 如果 Leader 的 term 不小于自己的，说明这是一个合法的 Leader，于是 Follower 更新自己的 term（如果更大的话）、退回 Follower 状态、记住这个 Leader 的 ID、重置选举定时器。

**第三步：校验"前一条日志"。** 这一步是保证日志链完整性的核心。Follower 检查自己在 `prevLogIndex` 这个位置上是否有一条日志，并且这条日志的 term 等于 `prevLogTerm`。这就像拼图——每一块新拼图必须对得上前一块。如果对不上，有两种可能：

- **Follower 日志滞后**：Follower 在 prevLogIndex 这个位置上根本没有日志（日志太短了）。返回失败。Leader 收到后会把这个 Follower 的 nextIndex 减一，然后重发。这个过程会一直重复——递减 nextIndex、重发、再递减——直到找到一个 Follower 和 Leader 在那个位置上日志一致的点，然后从那里开始补齐所有缺失的日志。

- **Follower 日志冲突**：Follower 在 prevLogIndex 位置上有一条日志，但 term 对不上。这说明 Follower 之前从一个旧 Leader 那里收到过日志，但那个旧 Leader 还没来得及提交就宕机了，现在新 Leader 的日志跟它不一样。处理方式一样，也是返回失败让 Leader 递减 nextIndex 重试。

**第四步：同步日志条目。** 前一条日志校验通过后，Follower 开始逐条处理 Leader 发来的日志。对于每个位置：
- 如果 Follower 在那个位置上有日志且 term 一致，跳过（已经同步过了，不重复写）
- 如果 Follower 在那个位置上有日志但 term 不一致，**截断**：从这个位置开始把后面的日志全部删掉，然后追加 Leader 的日志。这就是处理"Follower 日志超前但内容不对"的场景。
- 如果 Follower 在那个位置上没有日志，直接追加。

**第五步：更新 commitIndex。** 如果 Leader 的 commitIndex 比 Follower 当前的大，Follower 更新自己的 commitIndex，然后把新提交的日志应用到状态机。

### 提交推进的安全规则

Leader 在推进 commitIndex 时有一个重要的约束：**只能通过当前任期的日志来推进 commitIndex，不能直接提交旧任期的日志。** 这是防止一种很微妙的安全性问题——在特定的 Leader 切换序列下，一条旧任期的日志即使已经被多数派复制，也可能被新 Leader 的日志覆盖掉。所以旧日志只能"搭车"被间接提交，当前任期的日志（哪怕是那条 no-op）才是那辆"车"。

具体实现上，Leader 从最新的日志往前遍历，对每个位置检查两个条件：这条日志是当前任期的吗？复制到的节点数达到多数派了吗？两个条件同时满足，commitIndex 才往前推进。

---

## 六、一些我自己想通的东西

**关于 term 的比较为什么在不同场景下逻辑不一样。** 在选举投票里，比较的是 Candidate 最后一条日志的 term 和 index，目的是判断"谁的日志更新更全"。在日志同步里，比较的是请求中的 term 和 prevLogTerm，目的是判断"你是不是合法的 Leader"以及"我们的日志链是不是一致的"。虽然都涉及 term，但问的问题完全不同。

**关于为什么选举保证了已提交数据不丢失。** 已提交的日志必然存在于多数派节点上。要当选需要多数派投票。投票需要日志不落后于投票者。多数派中必然有拥有已提交日志的节点，而 Candidate 的日志必须不落后于这个节点才能拿到它的票。所以当选的 Leader 一定拥有所有已提交的日志。这是一个环环相扣的推导链。

**关于"最终一致性"这个词。** 以前觉得最终一致性就是"数据可能不对但最终会对"，听起来很不靠谱。学完 Raft 之后理解到，所谓最终一致性是有严格保证的——所有已提交的日志最终一定会被所有节点按相同顺序应用到状态机，因此所有节点的状态机最终一定收敛到相同的状态。"最终"指的是有一个确定的时间窗口（通常就是几轮心跳），不是"不知道什么时候"。

**关于 lastLogTerm 到底是什么时候写进去的。** 前面花了不少篇幅讲 lastLogTerm 在选举投票里有多重要，但一直没交代它的值是从哪来的。其实很简单：每条日志在被创建的时候就带上了它所属的 term，这个 term 就是当时 Leader 的 currentTerm。也就是说，只有 Leader 才会产生新日志，Leader 在 `append(currentTerm, command)` 的时候把自己当前的任期号盖在了这条日志上，就像给这条日志盖了个时间戳一样。之后不管这条日志被复制到哪个 Follower，这个 term 标记永远不变。

所以 `lastLogTerm` 的含义就是"这个节点最后一条日志是在哪个任期里被 Leader 写入的"。它不会因为节点自己选举自增 term 而改变——节点把自己的 currentTerm 从 3 刷到 100，但只要没有新日志写入，lastLogTerm 还是 3。只有当这个节点作为 Leader 真正写入了一条新日志，或者作为 Follower 从 Leader 那里同步到了一条新日志，lastLogTerm 才会更新到那条新日志的 term 值。

这也是为什么一个被网络隔离的节点虽然 term 很高但 lastLogTerm 很低——它隔离期间既当不了 Leader（拿不到多数票），也收不到 Leader 的日志同步，所以最后一条日志纹丝不动，lastLogTerm 停留在隔离之前的值。

**关于网络分区期间到底发生了什么。** 把上面的分析串起来想一下分区的完整场景。假设集群被网络切成了两半，Leader 在 A 分区，B 分区的几个节点失去了 Leader 的心跳。B 分区的节点选举定时器超时后会开始尝试选举，但它们永远也选不出 Leader——因为 Raft 的"多数派"是按**整个集群的总节点数**算的，不是按"当前能通信的节点数"算的。5 个节点的集群，多数派是 3，就算 B 分区有 2 个节点互相投票，也凑不齐 3 票。这就是 Raft 天然防脑裂的机制——少数派分区不可能选出 Leader，不可能接受写入，不可能产生跟 A 分区冲突的数据。

所以 B 分区在隔离期间就是不断地超时、发起选举、拿不到多数票、退回 Follower、再超时、再选举……一轮一轮空转。如果有 Pre-Vote 机制，这些节点连真正的选举都不会发起（Pre-Vote 也需要多数派认可），currentTerm 也不会被刷高，非常干净。如果没有 Pre-Vote，它们每轮选举都会自增 term，但除此之外什么也不会改变——日志不会增加，因为写请求只有 Leader 能处理，Follower 收到写请求要么转发给 Leader（但联系不到了），要么直接报错；日志同步也不可能发生，因为没有 Leader 发 AppendEntries。所以这些节点变化的只有 currentTerm 这一个数字，日志、状态机、commitIndex 全都冻结在分区前的状态。分区恢复后这些节点带着旧日志去竞选（term比较大嘛），第三步 lastLogTerm 比正常节点低，直接被拒绝，当不了 Leader。

那 A 分区呢？如果 Leader 在 A 分区，并且 A 分区本身的节点数就够多数派（比如 5 节点集群里 A 有 3 个），那 A 分区可以正常工作——读写请求照常处理，日志正常复制和提交，客户端感知不到集群出了问题。但如果 A 分区的节点数也不够多数派（比如 5 节点被切成了 2+2+1 三块），那 Leader 虽然还在，但它的日志复制也凑不齐多数派确认，commitIndex 推不动，写请求会一直卡着直到超时返回失败。更极端的情况是 Leader 自己也宕机了，那就彻底群龙无首——所有分区都选不出新 Leader，整个集群的写能力完全丧失，只能等分区恢复或者 Leader 重启。这种时候只有最终一致性读还能勉强工作（读各节点本地的状态机），线性一致性读也没法用了。







- Raft 真的必须是奇数节点吗？并不是。 

```
Raft 算法完全支持偶数个节点（比如 4 个）。

只不过在工程实践中，4 节点集群的多数派是 3（$4/2 + 1$），这意味着它最多只能容忍 1 个节点宕机。

而 3 节点集群的多数派是 2（$3/2 + 1$），同样能容忍 1 个节点宕机。既然 4 个节点和 3 个节点的容灾能力一模一样，还会白白多消耗一台机器的成本，所以我们在部署时才会主动选择奇数节点。

但在算法的逻辑推导里，并不存在“奇数节点”这个硬性前提。

2. 为什么同时加 2 台机器会引发“双主”（Split-Brain）？

你刚才推导的逻辑基于一个隐含前提：所有节点眼里的“集群总数”是一样的。但在动态添加节点的瞬间，这个前提被打破了。

配置信息（集群里到底有几台机器）本质上也是一条日志，它需要在网络中进行复制。网络是有延迟的，这就导致在某一个短暂的中间状态，集群里的节点会对“当前到底有几台机器”产生认知分歧。

我们可以推导一下这个危险的瞬间：初始状态 ($C_{old}$)：集群有 3 个节点 A、B、C。多数派是 2。目标状态 ($C_{new}$)：你想加入 D 和 E，变成 5 个节点。多数派是 3。危险发生：管理员发出了变更命令，配置日志开始同步。假设此时 A 和 B 还没收到新配置（它们认为集群总数还是 3），而 C、D、E 收到了新配置（它们认为集群总数变成了 5）。

如果在这个瞬间，原来的 Leader 宕机，触发了重新选举：节点 A 发起选举： 它按照 $C_{old}$ 的规则行事，认为只要拿到 2 票就能当选。它投自己 1 票，B 投给它 1 票。A 成功当选 Leader。节点 C 发起选举： 它按照 $C_{new}$ 的规则行事，认为需要拿到 3 票才能当选。它投自己 1 票，D 和 E 投给它。C 也成功当选 Leader。

这就出现了灾难性的后果：因为新旧配置的多数派要求不同（一个要 2，一个要 3），它们凑巧在 $\{A, B, C\}$ 和 $\{A, B, C, D, E\}$ 这两个不同的认知集合里，分别达到了“多数派”的标准，且这两个多数派群体没有任何交集。

3. Raft 的解法为了防止这种“认知分歧”导致脑裂，Raft 提供了两种标准的解法：

解法一：单节点变更（Single-Node Change）这是 Raft 作者后来极力推荐的工程简化版。核心思想是：每次只允许增加或删除 1 个节点。这背后的数学逻辑非常优雅：只要每次只变动 1 个节点，那么 $C_{old}$ 的多数派和 $C_{new}$ 的多数派必定存在交集。比如从 3 变 4，旧多数派是 2，新多数派是 3，你不可能在 4 个节点里凑出一个 2 人团体和一个 3 人团体且它们互不重叠。通过把“加两台机器”拆分成“加一台，等提交完毕，再加一台”，就从数学根源上扼杀了双主的可能。


解法二：共同一致性（Joint Consensus）这是原版论文里提出的两阶段提交法。Leader 不会直接把集群从 $C_{old}$ 切到 $C_{new}$，而是先下发一个中间过渡状态，叫做 $C_{old,new}$。当节点处于这个过渡状态时，任何日志的提交或选举，都必须同时满足 $C_{old}$ 的多数派和 $C_{new}$ 的多数派（也就是既要拿到 ABC 里的两票，也要拿到 ABCDE 里的三票）。这相当于用强制的“双重多数派”锁死了选举过程，等到这个过渡状态在集群中被多数派确认后，再安全地切换到单阶段的 $C_{new}$。

```
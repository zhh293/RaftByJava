# 我眼里的 Raft

> 这是我在学习 Raft 算法、阅读研究提纲、对照代码实现后，用自己的话梳理出来的理解。不追求学术严谨，但追求把每个环节想明白。

---

## 一、为什么要选主

分布式系统里多个节点都能接收写请求的话，冲突是必然的——两个节点同时收到对同一个 key 的写操作，到底听谁的？这个问题没有好的分布式解法，最简单的办法就是不让这个问题出现：所有写操作都由一个节点统一处理，其他节点只负责同步。这个唯一能处理写操作的节点就是 Leader。

所以选主这件事的根本目的就是**避免写冲突**，毋庸置疑。

---

## 二、选主的完整过程

### 初始状态

所有节点刚启动的时候都是 Follower，每个节点启动一个选举定时器，超时时间在一个区间内随机取值（比如 300ms 到 500ms 之间）。随机是关键——如果所有节点的超时时间一样，大家同时发起选举，谁都拿不到多数票，就僵住了。

### 发起选举

某个节点的选举定时器先到时间，它就准备竞选了。但这里有一个前置步骤：**Pre-Vote（预投票）**。

为什么需要 Pre-Vote？想象一个场景：集群正常运行着，某个节点因为网络分区被隔离了。它收不到 Leader 的心跳，就会不断超时、不断发起选举、不断自增 term。等网络恢复的时候，它带着一个异常高的 term 回来了，这个高 term 会迫使正常的 Leader 退位（因为 Raft 的规则是看到更高的 term 就退位），触发一次完全没必要的全集群重新选举。在选举期间，写请求没法处理，平白浪费了可用性。

Pre-Vote 就是解决这个问题的。节点不直接发起真正的选举，而是先"试探性地问一圈"：我打算竞选了，你们觉得我有戏吗？只有多数派回复说"你可以试试"，它才真正自增 term 发起选举。这样被隔离的节点发出的 Pre-Vote 请求根本得不到多数派响应（因为它跟大分区通信不了），也就永远不会真正自增 term，网络恢复后不会捣乱。

**那已经同步到正常节点上的数据会不会因为这次无意义的选举而丢失？** 不会。即使没有 Pre-Vote 保护，真的触发了重新选举，选举规则保证了当选的节点必须拥有最新的已提交日志。那个分区回来的节点 term 虽然高，但日志很旧，投票时其他节点一比较日志新旧就会拒绝给它投票，它当不了 Leader。所以 Pre-Vote 要防止的不是数据丢失，而是一次无意义的服务中断。

### Pre-Vote 通过后的真正选举

Pre-Vote 拿到多数派认可之后，节点才开始真正的选举：

1. **term++**：把自己的任期加一，进入新任期
2. **给自己投一票**
3. **角色切换为 Candidate**
4. **向所有其他节点广播投票请求（RequestVote）**，请求里携带自己的 term、最后一条日志的 term 和 index

同时启动一个**竞选超时定时器**，这个和选举超时定时器是分开的。选举超时是 Follower 等 Leader 心跳用的，竞选超时是 Candidate 等投票结果用的。如果竞选超时了还没凑齐多数票，说明这轮没戏，退回 Follower 重新等选举超时。

### 其他节点收到投票请求后怎么判断

这是整个选举机制里最核心的判断逻辑。收到 RequestVote 请求后，节点**按顺序**做以下检查——顺序很重要，前面的不通过，后面的根本不看：

**第一步：比 term。** 如果 Candidate 的 term 比自己的小，直接拒绝。term 小意味着这个 Candidate 的信息是过时的，它可能中间宕机过、被网络分区隔离过，总之它不了解集群的最新状况。不过如果 Candidate 的 term 比自己的大，节点要先把自己的 term 更新到 Candidate 的值，然后退回 Follower 状态（不管自己之前是什么角色），再继续后面的判断。

**第二步：查投票记录。** 在同一个 term 里，每个节点只能投一票。如果自己已经投过票了（votedFor 不为空），并且投的不是这个 Candidate，直接拒绝。这条规则是"一个任期只能选出一个 Leader"的基石。

**第三步：比日志新旧。** 这一步的判断规则稍微复杂一点，也是最容易搞混的地方。

首先要特别注意：**这里比较的 lastLogTerm 和前面第一步比较的 term 是完全不同的两个东西。** 第一步的 term 是 Candidate 的"当前任期号"，代表它经历了多少轮选举；而 lastLogTerm 是它最后一条日志所属的任期，代表它实际参与了多少轮有效的日志写入。这两者可以差很远——比如一个被网络隔离的节点不断超时选举，把自己的 term 刷到了 100，但因为隔离期间根本收不到任何日志，它的 lastLogTerm 可能还停留在 3。所以"term 高"不等于"日志新"。

比较规则是这样的：

- 先比最后一条日志的 **lastLogTerm**。Candidate 的 lastLogTerm 比自己的大，说明 Candidate 实际参与过更新任期的日志写入，日志更新，通过。
- 如果 lastLogTerm 相同，再比最后一条日志的 **lastLogIndex**。Candidate 的 lastLogIndex 不小于自己的，说明 Candidate 的日志至少和自己一样长，通过。
- 其他情况都是"Candidate 的日志比自己旧"，拒绝。

注意这里是**先比 lastLogTerm 再比 lastLogIndex**。为什么不直接只比 index 呢？因为 index 大不代表日志就更"正确"——一个节点可能在一个旧 term 里积累了很多未提交的日志（比如它曾经是 Leader 但还没来得及提交就宕机了），index 很大但都是无效的。lastLogTerm 才是更可靠的新旧指标。

回到前面那个 Pre-Vote 的例子：被隔离的节点即使没有 Pre-Vote 保护，带着 term=100 回来发起选举，第一步（比请求 term）和第二步（查投票记录）确实都能过，但到第三步就卡住了——它的 lastLogTerm 可能只有 3，而正常节点跟着正常 Leader 工作，lastLogTerm 已经到了 5、6、7，3 比 5 小，直接拒绝。它凑不齐多数票，当不了 Leader。所以 Pre-Vote 防的不是数据安全问题，而是那个高 term 会逼迫正常 Leader 在第一步就退位（看到更高 term 必须 stepDown），导致一段无谓的服务中断。

只有上面三步全部通过，节点才会投赞同票，同时记录下自己投给了谁（持久化到磁盘，防止重启后重复投票），并重置自己的选举定时器（既然投了票，说明有人在竞选了，自己就不急着超时了）。

**这里有一个我之前搞混的问题：怎么保证一个任期只有一个 Candidate 在选举？** 实际上根本不保证这一点。同一个 term 里完全可以有多个节点同时变成 Candidate 发起选举，这就是所谓的"选票瓜分"。安全性靠的不是"只有一个 Candidate"，而是"一票只投一人"加上"需要多数派同意"。两个多数派之间必然有交集（这是数学事实），所以不可能有两个 Candidate 同时拿到多数票。选票瓜分的结果就是大家都凑不齐多数票，这一轮作废，等竞选超时后退回 Follower，靠随机的选举超时让下一轮大概率只有一个节点先超时。

### Candidate 等待结果

Candidate 广播完投票请求后，等待各节点的回复，统计赞同票和拒绝票：

- **赞同票达到多数派**（超过半数）：竞选成功，调用 `becomeLeader()`，切换为 Leader。
- **拒绝票达到多数派**：不用再等了，不可能赢了，直接退回 Follower，重置选举定时器。这比傻等竞选超时要高效。
- **竞选超时**：没有明确结果（比如一些节点没回复），退回 Follower，重新来。

### 成为 Leader 后的第一件事

新 Leader 上任后会立即追加一条**空日志（no-op）**并广播给所有 Follower。这不是可有可无的，而是安全性必需的。原因比较微妙：Raft 规定 Leader 只能通过提交**当前任期**的日志来推进 commitIndex，不能直接提交旧任期的日志。新 Leader 上来如果不写一条自己任期的日志，就没法推进提交，那些旧任期遗留的已复制但未提交的日志就卡住了。no-op 就是来"占个位"的，让新任期有东西可以提交，旧日志也跟着一起被间接提交了。

---

## 三、Leader 宕机了怎么办

首先是某个 Follower 感知到 Leader 宕机了——具体说就是选举定时器超时了，一直没收到 Leader 的心跳。然后走上面那套完整的选举流程（Pre-Vote → 真正选举 → 上位）。

在这段没有 Leader 的时间里，**读请求要分情况**：如果客户端要的是最终一致性读（不要求最新数据），那读请求打到任何一个 Follower 都能正常返回，因为 Follower 的状态机虽然可能稍微落后，但数据是有效的。如果要的是线性一致性读，那就没办法了，必须等新 Leader 选出来。**写请求**则一律要返回错误或者告诉客户端"正在选举，请稍后重试"。

如果之后宕机的 Leader 恢复了（重启或者网络分区恢复），它也不会继续当 Leader。因为 Leader 通过心跳跟其他节点保持联系，心跳里带着 term 信息。新 Leader 上任后 term 已经更高了，旧 Leader 一旦收到任何包含更高 term 的消息（可能是新 Leader 的心跳，也可能是某个节点的投票请求），就会发现自己的 term 落后了，立即退位成为 Follower。这个退位检查遍布代码的各个消息处理入口——收到 AppendEntries、RequestVote、PreVote 响应时都会检查 term，只要发现更高的 term 就 stepDown，没有例外。

---

## 四、日志的读取

读请求分两种模式。

### 线性一致性读（Linearizable Read）

要求读到的一定是最新已提交的数据，不能有任何过时。这种读请求**必须由 Leader 处理**。

如果请求打到了一个 Follower，Follower 会返回一个重定向，告诉客户端 Leader 是谁，让客户端自己去找 Leader。

但 Leader 也不能直接读了就返回——它有可能已经被网络分区隔离了，自己还不知道集群已经选出了新 Leader（"不知大清已亡"）。如果直接返回，可能返回的是过时数据。所以 Leader 在处理线性一致性读之前，会先做一个**ReadIndex 确认**：记下当前的 commitIndex 作为"我要保证至少读到这个版本"的水位，然后向所有 Follower 发一轮心跳。只有多数派回复了心跳确认（证明"大清还在"），并且状态机已经把 commitIndex 对应的日志都应用完了，才从状态机读取数据返回给客户端。

### 最终一致性读（Eventual Consistency Read）

不要求一定是最新数据，允许稍微落后。这种读请求**任意节点都可以服务**，直接从本地状态机读就行。

但这里有一个问题：**客户端怎么避免"读回退"？** 比如客户端第一次读到了一个比较新的值（来自一个追赶得快的节点），第二次请求打到了一个落后的节点，反而读到了旧值。

解决方案是**appliedIndex 水位追踪**。工作原理是这样的：每次读响应里会带上当前节点的 `appliedIndex`（状态机已经应用到了哪一条日志）。客户端把这个值记下来。下一次读的时候把上次拿到的 `appliedIndex` 作为 `minAppliedIndex` 带在请求里，意思是"我至少要读到这个版本的数据"。服务节点收到后，看一下自己的 `lastApplied` 有没有达到这个要求——如果达到了，正常返回数据和更新后的 `appliedIndex`；如果没达到，返回一个"我这里数据还不够新，你换个节点试试"的拒绝响应。这样客户端读到的数据就只会越来越新，不会回退，实现了**单调读**的保证。

当然，这个前提是客户端愿意配合维护这个水位。如果客户端不带 `minAppliedIndex`，那就是纯粹的最终一致性，可能读到旧数据，但不会读到错误数据。

---

## 五、日志的写入

写请求必须由 Leader 处理，这是 Raft 的基本规则。如果客户端把写请求打到了一个 Follower，Follower 不会拒绝客户端，而是会**自动转发给 Leader**。具体实现是 Follower 给这次转发生成一个标识（forwardingId），记住原始客户端的连接通道，然后把请求发给 Leader。Leader 处理完之后响应里带着同样的 forwardingId 回来，Follower 再通过这个标识找到原始客户端的连接通道，把结果中继回去。对客户端来说是完全透明的，不需要知道集群的拓扑结构。如果 Follower 连 Leader 是谁都不知道（正在选举），那就只能返回错误让客户端重试了。

### 写入的两阶段提交

Leader 收到写请求后的处理流程：

**第一阶段（Propose 提议）**：Leader 把命令写入自己的预写日志（WAL），给它分配当前 term 和递增的 index，然后把这条日志通过 AppendEntries RPC 广播给所有 Follower。注意这里 Leader 不会立即响应客户端——它把这个"还没完成"的写请求挂在一个 pendingWrites 队列里等着。

**第二阶段（Commit 提交）**：Follower 收到日志后会校验（下面详细说），校验通过就追加到自己的日志里并返回成功。Leader 收集到**多数派确认**后，推进 commitIndex，然后按顺序把日志应用到状态机，再从 pendingWrites 里找到对应的客户端请求，返回成功响应。

如果 Leader 在日志提交前宕机了，pendingWrites 里的所有等待中的客户端请求都会收到一个"Leader 退位了"的错误，客户端需要自行重试。新选出的 Leader 会继续完成那些已经被多数派复制但还没提交的日志。

### Follower 收到日志同步请求后的校验

这里涉及到 term 和 index 的比较，但**跟选举投票时的比较逻辑完全不同**，不要搞混了。

AppendEntries 请求里带着 Leader 的 term、prevLogIndex（"我要从这个位置之后开始追加"）、prevLogTerm（"这个位置上的日志应该是这个 term 的"）、以及要追加的日志条目。

Follower 的校验顺序：

**第一步：比请求中的 term。** 如果 Leader 的 term 比自己的小，说明这个 Leader 是过时的，直接拒绝。并且在响应里带上自己的 term，Leader 收到这个响应后会发现 term 落后了，立刻退位。

**第二步：认可 Leader 权威。** 如果 Leader 的 term 不小于自己的，说明这是一个合法的 Leader，于是 Follower 更新自己的 term（如果更大的话）、退回 Follower 状态、记住这个 Leader 的 ID、重置选举定时器。

**第三步：校验"前一条日志"。** 这一步是保证日志链完整性的核心。Follower 检查自己在 `prevLogIndex` 这个位置上是否有一条日志，并且这条日志的 term 等于 `prevLogTerm`。这就像拼图——每一块新拼图必须对得上前一块。如果对不上，有两种可能：

- **Follower 日志滞后**：Follower 在 prevLogIndex 这个位置上根本没有日志（日志太短了）。返回失败。Leader 收到后会把这个 Follower 的 nextIndex 减一，然后重发。这个过程会一直重复——递减 nextIndex、重发、再递减——直到找到一个 Follower 和 Leader 在那个位置上日志一致的点，然后从那里开始补齐所有缺失的日志。

- **Follower 日志冲突**：Follower 在 prevLogIndex 位置上有一条日志，但 term 对不上。这说明 Follower 之前从一个旧 Leader 那里收到过日志，但那个旧 Leader 还没来得及提交就宕机了，现在新 Leader 的日志跟它不一样。处理方式一样，也是返回失败让 Leader 递减 nextIndex 重试。

**第四步：同步日志条目。** 前一条日志校验通过后，Follower 开始逐条处理 Leader 发来的日志。对于每个位置：
- 如果 Follower 在那个位置上有日志且 term 一致，跳过（已经同步过了，不重复写）
- 如果 Follower 在那个位置上有日志但 term 不一致，**截断**：从这个位置开始把后面的日志全部删掉，然后追加 Leader 的日志。这就是处理"Follower 日志超前但内容不对"的场景。
- 如果 Follower 在那个位置上没有日志，直接追加。

**第五步：更新 commitIndex。** 如果 Leader 的 commitIndex 比 Follower 当前的大，Follower 更新自己的 commitIndex，然后把新提交的日志应用到状态机。

### 提交推进的安全规则

Leader 在推进 commitIndex 时有一个重要的约束：**只能通过当前任期的日志来推进 commitIndex，不能直接提交旧任期的日志。** 这是防止一种很微妙的安全性问题——在特定的 Leader 切换序列下，一条旧任期的日志即使已经被多数派复制，也可能被新 Leader 的日志覆盖掉。所以旧日志只能"搭车"被间接提交，当前任期的日志（哪怕是那条 no-op）才是那辆"车"。

具体实现上，Leader 从最新的日志往前遍历，对每个位置检查两个条件：这条日志是当前任期的吗？复制到的节点数达到多数派了吗？两个条件同时满足，commitIndex 才往前推进。

---

## 六、一些我自己想通的东西

**关于 term 的比较为什么在不同场景下逻辑不一样。** 在选举投票里，比较的是 Candidate 最后一条日志的 term 和 index，目的是判断"谁的日志更新更全"。在日志同步里，比较的是请求中的 term 和 prevLogTerm，目的是判断"你是不是合法的 Leader"以及"我们的日志链是不是一致的"。虽然都涉及 term，但问的问题完全不同。

**关于为什么选举保证了已提交数据不丢失。** 已提交的日志必然存在于多数派节点上。要当选需要多数派投票。投票需要日志不落后于投票者。多数派中必然有拥有已提交日志的节点，而 Candidate 的日志必须不落后于这个节点才能拿到它的票。所以当选的 Leader 一定拥有所有已提交的日志。这是一个环环相扣的推导链。

**关于"最终一致性"这个词。** 以前觉得最终一致性就是"数据可能不对但最终会对"，听起来很不靠谱。学完 Raft 之后理解到，所谓最终一致性是有严格保证的——所有已提交的日志最终一定会被所有节点按相同顺序应用到状态机，因此所有节点的状态机最终一定收敛到相同的状态。"最终"指的是有一个确定的时间窗口（通常就是几轮心跳），不是"不知道什么时候"。

**关于 lastLogTerm 到底是什么时候写进去的。** 前面花了不少篇幅讲 lastLogTerm 在选举投票里有多重要，但一直没交代它的值是从哪来的。其实很简单：每条日志在被创建的时候就带上了它所属的 term，这个 term 就是当时 Leader 的 currentTerm。也就是说，只有 Leader 才会产生新日志，Leader 在 `append(currentTerm, command)` 的时候把自己当前的任期号盖在了这条日志上，就像给这条日志盖了个时间戳一样。之后不管这条日志被复制到哪个 Follower，这个 term 标记永远不变。

所以 `lastLogTerm` 的含义就是"这个节点最后一条日志是在哪个任期里被 Leader 写入的"。它不会因为节点自己选举自增 term 而改变——节点把自己的 currentTerm 从 3 刷到 100，但只要没有新日志写入，lastLogTerm 还是 3。只有当这个节点作为 Leader 真正写入了一条新日志，或者作为 Follower 从 Leader 那里同步到了一条新日志，lastLogTerm 才会更新到那条新日志的 term 值。

这也是为什么一个被网络隔离的节点虽然 term 很高但 lastLogTerm 很低——它隔离期间既当不了 Leader（拿不到多数票），也收不到 Leader 的日志同步，所以最后一条日志纹丝不动，lastLogTerm 停留在隔离之前的值。

**关于网络分区期间到底发生了什么。** 把上面的分析串起来想一下分区的完整场景。假设集群被网络切成了两半，Leader 在 A 分区，B 分区的几个节点失去了 Leader 的心跳。B 分区的节点选举定时器超时后会开始尝试选举，但它们永远也选不出 Leader——因为 Raft 的"多数派"是按**整个集群的总节点数**算的，不是按"当前能通信的节点数"算的。5 个节点的集群，多数派是 3，就算 B 分区有 2 个节点互相投票，也凑不齐 3 票。这就是 Raft 天然防脑裂的机制——少数派分区不可能选出 Leader，不可能接受写入，不可能产生跟 A 分区冲突的数据。

所以 B 分区在隔离期间就是不断地超时、发起选举、拿不到多数票、退回 Follower、再超时、再选举……一轮一轮空转。如果有 Pre-Vote 机制，这些节点连真正的选举都不会发起（Pre-Vote 也需要多数派认可），currentTerm 也不会被刷高，非常干净。如果没有 Pre-Vote，它们每轮选举都会自增 term，但除此之外什么也不会改变——日志不会增加，因为写请求只有 Leader 能处理，Follower 收到写请求要么转发给 Leader（但联系不到了），要么直接报错；日志同步也不可能发生，因为没有 Leader 发 AppendEntries。所以这些节点变化的只有 currentTerm 这一个数字，日志、状态机、commitIndex 全都冻结在分区前的状态。分区恢复后这些节点带着旧日志去竞选（term比较大嘛），第三步 lastLogTerm 比正常节点低，直接被拒绝，当不了 Leader。

当然，上面是说的少数派全部是follower的情况。

那 A 分区呢？如果 Leader 在 A 分区，并且 A 分区本身的节点数就够多数派（比如 5 节点集群里 A 有 3 个），那 A 分区可以正常工作——读写请求照常处理，日志正常复制和提交，客户端感知不到集群出了问题。但如果 A 分区的节点数也不够多数派（比如 5 节点被切成了 2+2+1 三块），那 Leader 虽然还在，但它的日志复制也凑不齐多数派确认，commitIndex 推不动，写请求会一直卡着直到超时返回失败。更极端的情况是 Leader 自己也宕机了，那就彻底群龙无首——所有分区都选不出新 Leader，整个集群的写能力完全丧失，只能等分区恢复或者 Leader 重启。这种时候只有最终一致性读还能勉强工作（读各节点本地的状态机），线性一致性读也没法用了。

上面这个是leader也在少数派，而且少数派不止一个的情况。






- Raft 真的必须是奇数节点吗？并不是。 

```
Raft 算法完全支持偶数个节点（比如 4 个）。

只不过在工程实践中，4 节点集群的多数派是 3（$4/2 + 1$），这意味着它最多只能容忍 1 个节点宕机。

而 3 节点集群的多数派是 2（$3/2 + 1$），同样能容忍 1 个节点宕机。既然 4 个节点和 3 个节点的容灾能力一模一样，还会白白多消耗一台机器的成本，所以我们在部署时才会主动选择奇数节点。

但在算法的逻辑推导里，并不存在“奇数节点”这个硬性前提。

2. 为什么同时加 2 台机器会引发“双主”（Split-Brain）？

你刚才推导的逻辑基于一个隐含前提：所有节点眼里的“集群总数”是一样的。但在动态添加节点的瞬间，这个前提被打破了。

配置信息（集群里到底有几台机器）本质上也是一条日志，它需要在网络中进行复制。网络是有延迟的，这就导致在某一个短暂的中间状态，集群里的节点会对“当前到底有几台机器”产生认知分歧。

我们可以推导一下这个危险的瞬间：初始状态 ($C_{old}$)：集群有 3 个节点 A、B、C。多数派是 2。目标状态 ($C_{new}$)：你想加入 D 和 E，变成 5 个节点。多数派是 3。危险发生：管理员发出了变更命令，配置日志开始同步。假设此时 A 和 B 还没收到新配置（它们认为集群总数还是 3），而 C、D、E 收到了新配置（它们认为集群总数变成了 5）。

如果在这个瞬间，原来的 Leader 宕机，触发了重新选举：节点 A 发起选举： 它按照 $C_{old}$ 的规则行事，认为只要拿到 2 票就能当选。它投自己 1 票，B 投给它 1 票。A 成功当选 Leader。节点 C 发起选举： 它按照 $C_{new}$ 的规则行事，认为需要拿到 3 票才能当选。它投自己 1 票，D 和 E 投给它。C 也成功当选 Leader。

这就出现了灾难性的后果：因为新旧配置的多数派要求不同（一个要 2，一个要 3），它们凑巧在 $\{A, B, C\}$ 和 $\{A, B, C, D, E\}$ 这两个不同的认知集合里，分别达到了“多数派”的标准，且这两个多数派群体没有任何交集。

3. Raft 的解法为了防止这种“认知分歧”导致脑裂，Raft 提供了两种标准的解法：

解法一：单节点变更（Single-Node Change）这是 Raft 作者后来极力推荐的工程简化版。核心思想是：每次只允许增加或删除 1 个节点。这背后的数学逻辑非常优雅：只要每次只变动 1 个节点，那么 $C_{old}$ 的多数派和 $C_{new}$ 的多数派必定存在交集。比如从 3 变 4，旧多数派是 2，新多数派是 3，你不可能在 4 个节点里凑出一个 2 人团体和一个 3 人团体且它们互不重叠。通过把“加两台机器”拆分成“加一台，等提交完毕，再加一台”，就从数学根源上扼杀了双主的可能。


解法二：共同一致性（Joint Consensus）这是原版论文里提出的两阶段提交法。Leader 不会直接把集群从 $C_{old}$ 切到 $C_{new}$，而是先下发一个中间过渡状态，叫做 $C_{old,new}$。当节点处于这个过渡状态时，任何日志的提交或选举，都必须同时满足 $C_{old}$ 的多数派和 $C_{new}$ 的多数派（也就是既要拿到 ABC 里的两票，也要拿到 ABCDE 里的三票）。这相当于用强制的"双重多数派"锁死了选举过程，等到这个过渡状态在集群中被多数派确认后，再安全地切换到单阶段的 $C_{new}$。

```

---

## 七、网络分区下的多数派博弈——Raft 的自愈能力

上面第六节已经讨论了网络分区的一般情况，这里用具体的数字把"多数派机制如何在分区下自动保证安全性和自动恢复"讲得更透彻一点。

### 场景一：5 节点集群，主节点 + 1 从节点 vs 3 从节点

下面是leader在少数派，多数派中全是follower的情况

假设集群有 A（Leader）、B、C、D、E 五个节点，网络分区把它们切成了两半：A 和 B 在一边，C、D、E 在另一边。

**大分区（C、D、E）这边：** 三个节点失去了 Leader 的心跳，选举定时器超时后开始竞选。5 节点集群的多数派是 3，C、D、E 加起来刚好 3 个节点，完全可以自行选出新 Leader。比如 C 超时最快，发起 Pre-Vote，D 和 E 都能收到，多数派通过；然后正式选举，C 投自己，D 和 E 投给 C（假设 C 的日志不落后），3 票达到多数派，C 成为新 Leader。之后 C、D、E 这个子集群可以正常接受写请求、复制日志、推进 commitIndex，对外完全可用。

**小分区（A、B）这边：** 旧 Leader A 还以为自己是 Leader，它尝试把新的写请求复制给 B，拿到了 A 自己 + B = 2 票确认，但多数派要 3 票，永远凑不齐。所以 A 的 commitIndex 推不动，所有写请求都会卡住直到超时返回失败。旧 Leader A 变成了一个"有名无实"的 Leader——它还保持着 Leader 状态，但什么也干不成。

**分区恢复后：** A 收到来自新 Leader C 的心跳（或任何 RPC），发现 C 的 term 比自己高（C 选举时 term 已经自增过了），A 立即执行 stepDown——退回 Follower，更新自己的 term，然后接受 C 的日志同步。如果 A 在分区期间有一些未提交的日志（只复制到了 A 和 B，没达到多数派），这些日志会被 C 的新日志覆盖掉（通过 AppendEntries 的一致性检查失败 → 回退 nextIndex → 截断冲突日志）。这不会造成数据丢失，因为这些日志从来没有被提交过，客户端早就收到了超时错误。

**本质上就是"旧主被夺权"**：多数派那边选出了合法的新主，旧主在少数派这边什么也干不了，分区恢复后自动退位，整个过程不需要任何人工介入。

### 场景二：3 节点集群，主节点 vs 2 从节点

下面这个例子同理也是

同样的道理。假设 A（Leader）被隔离，B 和 C 在另一边。3 节点集群多数派是 2，B 和 C 加起来刚好 2 个节点，可以选出新 Leader。旧 Leader A 孤零零一个人，1 票永远达不到多数派 2 票的要求，写请求全部失败。分区恢复后 A 看到更高的 term，立即退位。

### 场景三：3 节点集群，三个节点完全互相隔离

这个是leader也在少数派，而且少数派不止一个的情况。

如果不仅是主从之间断了，三个节点之间两两都不通，那就谁都选不出来了。每个节点最多只有自己的 1 票，达不到多数派 2 票的要求。整个集群完全不可用——这是 CAP 定理中选择 CP 的代价：为了一致性（C）和分区容忍性（P），牺牲了可用性（A）。只能等网络恢复，至少两个节点能互相通信后，才能重新选主恢复服务。

### 总结：Raft 用多数派机制实现了什么

这几个场景串起来看，Raft 的多数派机制天然地解决了三个问题：

1. **防脑裂**：任何两个多数派之间必然有交集（数学事实），所以不可能同时存在两个合法的 Leader。
2. **自动故障转移**：只要多数派存活且互通，就能选出新 Leader 继续服务，不需要人工介入。
3. **自动恢复**：分区恢复后，旧 Leader 通过 term 比较自动退位，通过日志同步自动对齐数据，整个过程完全自愈。

这就是为什么 Raft 被认为是"易于理解"的共识算法——它的每一个设计决策都指向同一个核心思想：**多数派不会同时存在两个，只要多数派还在，系统就是安全的。**

---

## 八、Leader 宕机后的数据安全与恢复机制

### 首先要澄清：Leader 的日志不一定是"物理上最新的"

Raft 的选举限制保证的是：**新 Leader 一定拥有所有已提交（committed）的日志**，但不保证它拥有所有节点中物理上最长的日志。

为什么？因为投票时比较的是 lastLogTerm/lastLogIndex，而已提交日志一定存在于多数派节点上。任何能拿到多数派投票的 Candidate，其日志一定不比多数派中任何一个节点旧——多数派投票集合和多数派提交集合必然有交集，交集中的节点不会给日志比自己旧的 Candidate 投票。所以当选者一定覆盖了所有已提交日志。

但是**未提交的日志**（只复制到了少数派、还没拿到多数派确认的），新 Leader 可能有也可能没有。如果没有，这些日志会在后续的日志同步中被新 Leader 的日志覆盖掉——这不算丢数据，因为这些日志从来没有对客户端承诺过"写入成功"。

### Leader 提交前宕机，数据会不会丢？

这里要分两种情况：

**情况一：日志已经复制到多数派，但 Leader 还没来得及推进 commitIndex 就挂了。** 这种情况数据不丢。新选出的 Leader 一定拥有这条日志——因为多数派里必然有持有这条日志的节点，它的 lastLogTerm/lastLogIndex 更高或相等，选举时的日志比较机制保证了它要么自己当选，要么当选的那个也有这条日志。新 Leader 上任后写一条 no-op，no-op 被多数派确认时 commitIndex 往前推，这条旧日志就被"搭车"间接提交了。

**情况二：日志只复制到了少数派（甚至只在旧 Leader 本地），Leader 就挂了。** 这种情况结果不确定，取决于谁当选新 Leader。如果当选的节点恰好有这条日志（比如它就是那个少数派之一），那这条日志会被保留下来，新 Leader 照样走 no-op 搭车提交的流程把它间接提交掉；如果当选的节点没有这条日志，后续日志同步时会把那些少数派节点上的多余日志截断覆盖掉。但无论哪种结果都**不违反正确性保证**——因为客户端从来没收到过"写入成功"的响应，从客户端视角这次写入就是失败了，数据保留下来是"运气好"，被覆盖掉也完全合法，客户端重试即可。

**提交应用后宕机**就更安全了——已提交意味着多数派都有这条日志，新 Leader 必然也有，继续正常工作就行。

核心点在于：**Raft 对客户端的承诺边界是 commit，不是 replicate。** 只有 commit 了的数据才保证不丢，commit 之前的数据处于"尽力而为"的状态。这也是为什么客户端必须等到收到"写入成功"响应后才能认为数据是安全的。

### 这跟 no-op 的设计完美呼应

前面第二节提到，新 Leader 上任后会立即追加一条 no-op。这个设计专门就是为了解决上述"情况一"——日志已复制到多数派但卡在"已复制未提交"的状态。新 Leader 不能直接说"我看到这条旧日志多数派都有了，提交吧"，因为 Raft 不允许直接提交旧任期的日志（Figure 8 的反例证明了直接提交旧日志会出问题）。所以必须写一条当前任期的 no-op，no-op 拿到多数派确认后 commitIndex 推到 no-op 的位置，前面所有旧日志自然就被"夹带"提交了。

完整的安全性链条：旧 Leader 宕机 → 新 Leader 当选（一定拥有所有已提交日志）→ 写 no-op（推进 commitIndex 的"车"）→ 旧日志搭车提交 → 已复制到多数派的数据不丢。每个设计都不是孤立的，都是在为某个具体的边界场景兜底。

### 新 Leader 上任时会主动探测日志一致性

新 Leader 上任后做三件事：初始化所有 Follower 的 nextIndex 为自己日志的尾后位置，发 no-op，然后**立即为每个 Follower 启动一个 Replicator 并主动发送一次 Probe 请求**。

这个 Probe 请求不是等心跳超时才触发的——在 SOFAJRaft 中，`Replicator.start()` 方法的最后一行就是 `r.sendProbeRequest()`，也就是说 Replicator 被创建出来的那一刻就立刻发了一个空的 AppendEntries RPC 去探测 Follower 的日志状态。这是一次**主动的、即时的一致性探测**，Leader 不会干等着下一次心跳或客户端写入才去发现谁的日志跟自己不一致。

所以准确的描述是：**"乐观探测 + 被动回退修复"**。Leader 上任后主动发 Probe 去试探每个 Follower（乐观假设大家都跟上了），如果 Probe 失败（Follower 一致性检查没通过），才进入回退逻辑——递减 nextIndex 重试，逐步退到两边一致的位置，然后从那里开始覆盖。回退这个过程确实是渐进的、响应式的，但触发它的第一枪是 Leader 主动打出去的，不是被动等来的。

这样设计比论文描述的纯惰性方案更激进也更高效：Leader 不需要等到有新日志可发的时候才顺便发现不一致，而是上任第一时间就开始并行探测所有 Follower，让不一致的节点尽早进入修复流程。对于日志本来就一致的 Follower，Probe 直接通过，随即进入正常 Pipeline 模式发日志；对于不一致的 Follower，Probe 失败后立刻开始回退，不用白白浪费一个心跳周期的时间。

### 为什么要把 nextIndex 初始化为尾后位置

因为 Leader 刚当选的时候，它不知道每个 Follower 的日志到底同步到哪里了——可能有的 Follower 完全跟上了，有的落后了很多，有的还有冲突日志。

把 nextIndex 统一初始化为自己日志的尾后位置，其实就是一个**"乐观假设"**：先假设所有 Follower 都跟我一样新。然后 Replicator 启动时发 Probe（一个携带 prevLogIndex=nextIndex-1 的空 AppendEntries），如果 Follower 确实跟上了，一致性检查直接通过，皆大欢喜，立即进入 Pipeline 复制模式；如果没跟上或者有冲突，一致性检查失败，nextIndex 再往回退，逐步找到正确的位置。

这就是乐观探测的起点——从最乐观的位置开始主动试探，比从最悲观的位置（比如从 0 开始往前扫）效率高得多，因为大多数情况下 Follower 的日志只是稍微落后一点，第一次 Probe 或者退几步就能对上了。关键区别在于：这个试探是 Leader 上任后**立即主动发起**的，不是等到有新写入或心跳超时才"顺便"触发的。

---

## 九、深入理解 no-op：为什么新 Leader 不能直接提交旧任期的日志

前面多次提到新 Leader 上任后必须先提交一条当前任期的 no-op，旧任期的日志只能"搭车"被间接提交。这个设计不是锦上添花，而是安全性的硬性要求。下面用一个具体的场景来说明：如果省略 no-op、直接提交旧任期日志，会导致什么灾难性的后果。

### 场景构造

假设节点 B 在任期 T+1 当选新 Leader，它的日志中继承了旧 Leader A 遗留的、尚未提交的任期 T 的日志条目。如果 B 直接把这些任期 T 的旧条目复制到多数派并推进 commitIndex，从表面上看这些日志已经"提交"了。然而此时 B 也宕机了，旧 Leader A 恢复上线。

问题出在选举限制的比较逻辑上。A 的日志可能比某些节点短，但 A 日志中最后一条的 lastLogTerm 同样是 T——与那些接收了旧条目但没有更高任期日志的节点的 lastLogTerm 相同。在 lastLogTerm 相同的情况下，选举比较退化为 lastLogIndex 的比较。如果集群中存在尚未收到复制的节点（它们的 lastLogIndex 更短），A 完全有可能从这些节点手中获得选票，凑齐多数派当选新 Leader。

A 一旦当选，它会通过 AppendEntries 的一致性检查将自己的日志强制同步到所有 Follower——这意味着那些已经被"提交"的任期 T 的日志条目会被截断覆盖。**已提交的日志被修改，这直接违反了 Raft 最核心的安全性保证。**

### no-op 如何从根本上封死这条路

新 Leader B 在任期 T+1 当选后，先追加一条当前任期 T+1 的 no-op 空日志，并将其复制到多数派使之提交。这条 no-op 提交的同时，它之前所有的旧条目（包括任期 T 的那些）也被间接提交了。

关键变化在于：此时多数派节点的 lastLogTerm 已经变成了 T+1。当旧 Leader A 恢复并试图发起选举时，它的 lastLogTerm 最多是 T，而多数派节点的 lastLogTerm 已经是 T+1。由于 T < T+1，在选举比较的第一优先级（lastLogTerm）上 A 就已经落败，多数派节点会直接拒绝给 A 投票，A 根本不可能获得多数票当选。

更进一步，即使 A 不是发起选举，而是以旧 Leader 的身份直接向其他节点发送 AppendEntries 同步请求，持有更高任期日志的节点会发现 A 的 term 小于自己的 currentTerm，直接拒绝该请求并在响应中告知自己的更高 term，迫使 A 退位为 Follower。

### 本质

no-op 的本质作用是：**将"旧任期条目的提交"转化为"当前任期条目的提交"，从而让 Raft 的选举限制能够保护这些已提交的条目不被后来的旧 Leader 覆盖。** 没有 no-op，已提交日志的 lastLogTerm 停留在旧任期，选举限制无法区分"持有已提交日志的节点"和"持有过时日志的旧 Leader"，安全性就会被击穿。有了 no-op，已提交日志的保护被锚定在当前最新任期上，任何持有旧任期的节点都不可能通过选举限制获得多数票，已提交的日志就永远安全了。

这正是 Raft 论文 Figure 8 所描述的经典反例场景，也是 Raft 设计中"Leader 只能通过提交当前任期的日志来间接提交旧任期日志"这条规则的根本原因。

---

## 十、心跳机制：Raft 中最被低估的多面手

学 Raft 的时候，最先接触到的就是"Leader 会定期发心跳给 Follower"。一开始我觉得心跳就是个存活检测——告诉 Follower"我还活着，别急着选举"。但越深入研究越发现，心跳在 Raft 里扮演的角色远不止这一个。它更像是一条贯穿整个协议的神经，几乎所有核心机制都以心跳为载体或触发条件。下面把心跳的所有作用系统性地整理出来。

### 10.1 心跳的物理本质：空的 AppendEntries RPC

在说作用之前，先搞清楚心跳到底"是什么"。Raft 论文里没有给心跳定义一种专门的 RPC 类型——心跳就是一个 `entries[]` 为空的 `AppendEntries RPC`。也就是说，Leader 往 Follower 发日志和发心跳用的是同一个接口，区别仅仅在于有没有携带日志条目。

这个设计非常精妙。因为 AppendEntries 请求本身就携带了 Leader 的 `term`、`leaderId`、`prevLogIndex`、`prevLogTerm`、`leaderCommit` 这些字段，心跳复用这个结构意味着每一次心跳都附带了大量的元信息，不是一个简单的"ping-pong"。正因如此，心跳才能同时承担下面列举的这么多职责。

### 10.2 作用一：维持 Leader 权威，抑制不必要的选举

这是最基础也最广为人知的作用。Raft 的选举是被"超时"驱动的——Follower 如果在选举超时时间内没有收到任何 Leader 的消息，就会认为 Leader 挂了，开始发起选举。心跳的第一职责就是在这个超时到来之前，让 Follower 刷新自己的选举定时器（electionElapsed 归零），告诉所有 Follower："Leader 还在，不用选举。"

Leader 发心跳的间隔（heartbeatInterval）必须远小于 Follower 的选举超时时间（electionTimeout）。论文建议的经验法则是：`broadcastTime << electionTimeout << MTBF`（节点平均故障间隔）。工程实践中，heartbeatInterval 通常设为 100ms~200ms，electionTimeout 设为 300ms~1500ms 的随机值。心跳间隔太长，Follower 容易误超时触发不必要的选举；太短，会增加网络流量。

核心要点是：**心跳不是可选的性能优化，而是 Raft 正确运行的硬性要求**——没有心跳，集群就会不断触发无意义的选举，完全无法稳定工作。

### 10.3 作用二：传播 Term，实现分布式"逻辑时钟"同步

心跳复用了 AppendEntries RPC，因此每一次心跳都携带了 Leader 的 `currentTerm`。这个 term 在 Raft 中充当逻辑时钟的角色，是整个协议判断"谁新谁旧"的核心依据。

当 Follower 收到心跳后，会把请求中的 term 与自己的 currentTerm 做比较。如果 Leader 的 term 更大，Follower 会更新自己的 term 到 Leader 的值。这意味着心跳持续地在集群中传播最新的 term 信息，让所有节点的逻辑时钟保持同步。

反过来，如果 Follower 发现心跳中的 term 比自己的小——比如一个旧 Leader 在网络分区恢复后仍然在发心跳——Follower 会拒绝这个心跳，并在响应中告知自己更高的 term。旧 Leader 收到响应后发现 term 落后了，立刻执行 stepDown 退位为 Follower。

所以心跳在 term 传播上是**双向的**：Leader → Follower 传播最新 term，Follower → Leader 的响应也可能携带更高 term 迫使 Leader 退位。这个机制保证了整个集群中"过时的 Leader"会被迅速发现并淘汰。

### 10.4 作用三：推进 Follower 的 commitIndex

AppendEntries RPC 中有一个 `leaderCommit` 字段，表示 Leader 当前已提交到哪一条日志。心跳虽然不携带日志条目，但这个 `leaderCommit` 字段依然会被发送。

Follower 收到心跳后，会把自己的 commitIndex 更新为 `min(leaderCommit, 自己最后一条日志的 index)`。然后 Follower 就可以把 commitIndex 之前那些"已复制但还没提交"的日志应用到状态机。

这是一个非常重要的作用。在前面第五节讲日志复制时提到，Leader 推进 commitIndex 发生在收到多数派确认之后，但 Follower 并不知道"多数派已经确认了"这件事——Follower 只知道自己已经把日志存下来了，至于其他节点是否也存了，它完全不清楚。Follower 的 commitIndex 完全依赖 Leader 通过 `leaderCommit` 字段告知。如果 Leader 只在日志复制的 AppendEntries 里携带 leaderCommit，那么在没有新写入的空闲期间，Follower 的 commitIndex 就没有机会更新。正是心跳持续地携带 leaderCommit，保证了即使在空闲期间 Follower 也能及时推进 commitIndex 并应用日志到状态机。

简单说：**心跳是 commitIndex 从 Leader 流向 Follower 的持续通道。** 没有心跳，Follower 的状态机推进就只能依赖客户端写入来"顺便"带上 commitIndex，空闲期间状态机就会停滞。

### 10.5 作用四：为 ReadIndex 提供 Leader 身份确认

这是前面第四节提到过但没深入展开的机制。线性一致性读要求 Leader 在读之前确认自己还是合法的 Leader，防止"大清已亡"的脑裂读。ReadIndex 的做法是：Leader 收到读请求时，记下当前的 commitIndex 作为 readIndex，然后向所有 Follower 发一轮心跳。只有多数派回复了心跳确认，Leader 才敢确定自己还是合法的 Leader，然后等状态机 apply 到 readIndex 的位置后返回读结果。

注意这里的心跳不是常规的定时心跳，而是一次**按需触发的额外心跳**——专门为了确认 Leader 身份而发的。虽然物理上它和普通心跳一模一样（都是空的 AppendEntries），但语义上它是 ReadIndex 流程中不可缺少的一步。

为什么要这么做？因为在 Raft 中，一个 Leader 被网络分区隔离后，它不会立即感知到自己已经不是 Leader 了——在它的选举超时到期之前，它还"认为"自己是 Leader。如果在这个窗口期内有客户端来读，它会返回可能已经过时的数据。ReadIndex 通过发一轮心跳并收到多数派确认，来确保"此刻此刻我确实还有多数派的支持"，才放心地读。

这也解释了为什么 ReadIndex 读虽然不需要写日志，但性能仍然不是免费的——它至少需要一个心跳往返的网络延迟（RTT）。

### 10.6 作用五：Lease Read 的租约基础

Lease Read 是 ReadIndex 的性能优化版。它的思路是：Leader 在最近一次心跳收到多数派确认后，可以认为在接下来的一个**租约期**内（通常设为比 electionTimeout 小一个数量级，比如 electionTimeout 的 1/3），不可能有新的 Leader 被选出来——因为其他节点的选举超时还没到期。在租约期内，Leader 处理读请求可以直接从本地状态机读取，连 ReadIndex 那一轮心跳确认都省了。

这里心跳的角色变了：它不再是每次读请求时的按需确认，而是租约的**时钟起点**。Leader 每次收到心跳的多数派确认时就"续租"，续租后的一段时间内读操作零网络开销。

但 Lease Read 有一个前提假设：各节点的时钟漂移在可接受的范围内。如果某台机器发生了严重的 GC 停顿（Stop-The-World）或者 CPU 时钟率异常，Leader 以为租约还没过期，但实际上其他节点的选举超时已经到了、新 Leader 已经选出来了，这时候读就会读到过时数据。所以 Lease Read 是用**时钟可靠性**换**读性能**的权衡——在 CPU 时钟稳定的环境下非常好用，在极端环境下可能违反线性一致性。

### 10.7 作用六：CheckQuorum——Leader 的自我健康检查

前面第二节讲 Pre-Vote 的时候提到了 CheckQuorum 机制，但当时只是一笔带过。这里详细说一下它和心跳的关系。

CheckQuorum 的逻辑是：Leader 在每个心跳周期发出心跳后，会统计有多少个 Follower 回复了确认。如果在连续若干个心跳周期（通常是一个 electionTimeout 的周期）内，Leader 发现自己无法从多数派获得心跳响应，它就会主动退位为 Follower。

这是一个**"领导者自知之明"机制**。在没有 CheckQuorum 的情况下，如果 Leader 被网络分区隔离到了少数派，它不会主动退位——它只会发现日志复制凑不齐多数派确认，写请求卡住，但它仍然"自认为"是 Leader。这个状态可能持续到分区恢复，期间如果有客户端找到了这个旧 Leader，会被误导。

有了 CheckQuorum，Leader 利用心跳响应的统计信息主动判断"我是不是已经跟多数派失联了"，如果是，就主动让位。这和 Pre-Vote 形成了完美的互补：Pre-Vote 防止少数派分区的节点"回来捣乱"（不让它们推高 term），CheckQuorum 防止被隔离的 Leader"赖着不走"（让它主动退位）。两者都依赖心跳作为信息载体。

### 10.8 作用七：探测 Follower 存活状态与日志进度

Leader 维护了每个 Follower 的 `nextIndex` 和 `matchIndex`。心跳（作为空的 AppendEntries）发出后，Follower 的响应会告诉 Leader "我目前的日志状态"。如果心跳一致性检查失败（Follower 的日志和 Leader 不一致），Leader 就知道这个 Follower 需要修复，于是进入回退逻辑——递减 nextIndex 重发，逐步找到一致点后补齐日志。

如果 Follower 长时间不回复心跳，Leader 就知道这个节点可能已经宕机或者网络不通。在有些实现中（如 SOFAJRaft），Leader 会据此决定是否需要给这个 Follower 发送 InstallSnapshot（如果 Follower 落后太多，需要的日志已经被 Leader 压缩掉了，就不能再通过 AppendEntries 逐条补了，只能一次性发快照）。

所以心跳不仅仅是一个"你还活着吗"的 ping，它更是 Leader 感知整个集群健康状态的**持续探测机制**：谁在线、谁掉线、谁的日志跟上了、谁需要修复、谁需要快照——这些信息全部来自心跳及其响应。

### 10.9 作用八：Leader Transfer 中的 TimeoutNow 触发

在运维场景中，有时候需要把 Leader 身份从一个节点平滑地转移到另一个节点（比如要对当前 Leader 机器做维护）。这就是 Leader Transfer。

Transfer 的流程中有一个关键步骤：Leader 先确保目标节点（Transferee）的日志追上自己，然后向 Transferee 发送一个特殊的消息（TimeoutNow），让它立即发起选举。这个 TimeoutNow 消息本质上相当于告诉 Transferee"不用等选举超时了，现在就当自己超时了，直接竞选吧"。

在 Transfer 期间，Leader 会**停止接受新的写入**（block proposal），然后通过加速发送 AppendEntries（本质上是把心跳中携带的日志加速发送）来帮 Transferee 追上日志进度。追平后发 TimeoutNow，Transferee 立即自增 term 发起选举，凭借最新的日志赢得选举成为新 Leader。旧 Leader 收到更高 term 的消息后自动退位。

虽然 TimeoutNow 不完全等同于心跳，但整个 Transfer 流程是建立在心跳基础设施之上的——日志追赶用的是 AppendEntries（心跳的"满载"形态），Transferee 能否顺利选举取决于它之前是否正常接收了心跳和日志同步。

### 10.10 作用九：Pre-Vote 阶段的间接依赖

前面第二节详细讲了 Pre-Vote 机制。这里补充一个和心跳有关的细节：当 Follower 收到 Pre-Vote 请求时，如果它**最近刚收到过 Leader 的心跳**（说明 Leader 还活得好好的），它会直接拒绝这个 Pre-Vote 请求——"Leader 还在，你为什么要选举？"

这意味着心跳还间接地为 Pre-Vote 提供了判断依据。只有当 Follower 确实一段时间没有收到心跳了（选举超时到期），它才会认可 Pre-Vote 请求并给出赞同。这进一步加强了集群的稳定性——即使某个节点因为临时的网络抖动没收到几次心跳就发起 Pre-Vote，其他刚收到过心跳的节点也会拒绝，不会被带节奏。

### 10.11 总结：心跳的九重身份

把上面的分析汇总一下，Raft 中心跳同时承担了以下职责：

1. **Leader 权威维持器**：定期重置 Follower 的选举定时器，抑制不必要的选举。
2. **逻辑时钟同步器**：通过 term 字段在集群中传播最新的任期信息，淘汰过时的 Leader。
3. **commitIndex 传播通道**：通过 leaderCommit 字段让 Follower 及时推进提交进度和状态机应用。
4. **ReadIndex 身份确认**：按需发起一轮心跳确认 Leader 合法性，保障线性一致性读。
5. **Lease Read 租约时钟**：心跳的多数派确认作为租约起点，在租约期内实现零网络开销读。
6. **CheckQuorum 自检信号**：Leader 通过心跳响应统计判断自己是否还与多数派保持连通。
7. **集群健康探测器**：感知每个 Follower 的在线状态和日志进度，触发修复或快照传输。
8. **Leader Transfer 基础**：日志追赶和 TimeoutNow 触发都建立在心跳/AppendEntries 之上。
9. **Pre-Vote 的隐性守卫**：最近收到心跳的节点会拒绝 Pre-Vote 请求，防止正常集群被干扰。

一个空的 AppendEntries RPC，不携带任何业务数据，却串联起了选举、日志复制、一致性读、成员管理、故障检测等几乎所有核心机制。这就是 Raft 设计的优雅之处——**用最简单的结构承载最多的语义**。心跳不是一个独立的子系统，它是整个 Raft 协议的脉搏。
















# 记录一下这两天vibe raft算法的时候给ai的提示词，感觉自己很伟大啊，哈哈，能切实地感觉到自己在进步



```
我现在想要为admin的配置中心做一个分布式高可用的架构，根基就是用raft算法来完成，实现思路是这样子的，admin中的配置可以类比为日志。ok，我来讲述一下这个raft算法的实现，首先开局就是每个节点启动计时器，倒计时时间是基础时间加上抖动时间，防止多个节点同时开始选举，到了时间之后节点从follower变为candidate，任期+1，然后开始选举，这个选举的过程我给你描述一下，节点在选举前会先pre-vote用来避免不必要的选举，，得到大多数结点的同意之后才开始正式的拉取选票，只要拿到大多数选票就可以立马晋升为leader，新leader会向其他节点发送通知，让他们把leader改为它，这个选举的判断标准是什么呢，首先看这个候选人的任期，如果小于等于自己（其他follow节点）的任期，那直接拒绝，然后就是看最新日志的任期是否小于自己，如果是的话就直接拒绝，最后再看谁具有更多的日志，如果候选人的日志更少的话，同样直接拒绝，这几个条件通过之后才可以投同意票，leader上任后会立刻发布一个no-ops用来推进日志更新，并且会让其他节点和自己的配置同步，具体措施我们下面会说。关于日志，日志的读取，一般就是所有节点都可读，只有leader可以写，写的话leader会先写入日志，然后等待半数以上节点都同意之后再提交并让所有节点提交应用这个新配置，节点收到这个日志同步消息后同样会校验，先看任期，比自己小的直接拒绝，然后就看这个新日志，如果超出自己的已有日志太多，会批量把没有日志让leader同步给你，如果这个日志滞后的话，就把你多出来的日志给截断，冲突日志不能笼统截断，只能在 `prevLogIndex/prevLogTerm` 校验后删除未提交的冲突后缀，已leader发来的这个日志为最新的。日志的读取：日志的读取在这里我还是建议线性一致读，就是我们必须要考虑这么一种情况，如果说leader收到大多数节点的响应并且成功应用之后，那还是有可能有节点没有成功应用这个新配置的啊，拿着时候就可能出现多次读取拿到的配置信息不一致，这是很可怕的一件事情，所以我们必须做一个约束，保证每次读取的配置一定是一样的，那我们该怎么做的，线性一致读，etcd的raft线性一致读就是采用readindex这么一个方法来保证的，具体我就不展开了，你肯定知道的。。。。。。。。。。。。。。。。。。。。。心跳检测，多节点的心跳检测是非常重要的，首先呢，这个心跳检测可以探测是否存活，是决定是否要发生选举的重要因素，双方都很依赖这个心跳检测，follower需要这个来探测leader是否还存活，leader需要依据这个来续约，每次租约快到期的时候会根据是否有半数以上节点回复它心跳，没有的话就不会续约，租期到了之后leader会退位等待重新选举（可能发生了网络分区），心跳还包含着任期，leader这些重要信息，恢复的老leader，恢复的follower，这个心跳的信息可以帮助他们退位，或者更新自身的任期，leader这些信息，还可以用来更新自己的日志，在空闲时候就直接更新了，避免在写入的时候才进行更新，影响性能和效率。。。。。。。。。。。。。。。。。。。。。脑裂问题，什么是脑裂，一个集群中出现了多个leader导致配置写入和读取出现不一致的情况，就是脑裂，但实际上这个问题在raft的框架下面被优雅的避免了，比如说我们发生了所谓的网络分区，1.leader在多数派分区中，这时候系统其实是可以正常使用的，另一半分区中由于数目不够加上pre-vote，肯定不会出现另一个leader。2.leader在少数派分区当中，那多数派中可能会出现这个新leader，但这是无伤大雅的，少数派分区的leader是无法工作的，只有新leader才可以正常的写入配置，并且还记得我们前面心跳检测的时候提出的租期概念吗，这个少数派分区的leader会随着租期到期自动退位，从而让少数派分区失去leader，在分区恢复后，两种情况下都可以通过心跳检测恢复正常状态，而如果网络被分成了三块，那基本上整个系统就是不可用的了，这就是cp系统，为了数据一致性而牺牲了可用性，但这是值得的。但是我们不得不考虑一个新的问题，就是对于分区过程中会在短暂的时间内仍然出现了两个leader，这个时候的读取和写入该怎么办呢，总得找到正确的leader吧，即便这段时间少数派leader也无法写入，但是我们也要尽可能保证可用性啊，这个问题其实我不是很清楚，之后就交给你吧。。。。。。。。。。。。。。。。。。。。。。节点扩缩容问题，这个问题很经典了，如果扩缩容结点的时候还能保证配置不混乱，这个raft作者提出了一个非常好的解决方案，&#x20;
&#x20;
配置信息（集群里到底有几台机器）本质上也是一条日志，它需要在网络中进行复制。网络是有延迟的，这就导致在某一个短暂的中间状态，集群里的节点会对“当前到底有几台机器”产生认知分歧。&#x20;
\&#x20;
我们可以推导一下这个危险的瞬间：初始状态 ($C\\\_{old}$)：集群有 3 个节点 A、B、C。多数派是 2。目标状态 ($C\\\_{new}$)：你想加入 D 和 E，变成 5 个节点。多数派是 3。危险发生：管理员发出了变更命令，配置日志开始同步。假设此时 A 和 B 还没收到新配置（它们认为集群总数还是 3），而 C、D、E 收到了新配置（它们认为集群总数变成了 5）。&#x20;
\&#x20;
如果在这个瞬间，原来的 Leader 宕机，触发了重新选举：节点 A 发起选举： 它按照 $C\\\_{old}$ 的规则行事，认为只要拿到 2 票就能当选。它投自己 1 票，B 投给它 1 票。A 成功当选 Leader。节点 C 发起选举： 它按照 $C\\\_{new}$ 的规则行事，认为需要拿到 3 票才能当选。它投自己 1 票，D 和 E 投给它。C 也成功当选 Leader。&#x20;
\&#x20;
这就出现了灾难性的后果：因为新旧配置的多数派要求不同（一个要 2，一个要 3），它们凑巧在 $\\\\{A, B, C\\\\}$ 和 $\\\\{A, B, C, D, E\\\\}$ 这两个不同的认知集合里，分别达到了“多数派”的标准，且这两个多数派群体没有任何交集。&#x20;
\&#x20;
3\\. Raft 的解法为了防止这种“认知分歧”导致脑裂，Raft 提供了两种标准的解法：&#x20;
\&#x20;
解法一：单节点变更（Single-Node Change）这是 Raft 作者后来极力推荐的工程简化版。核心思想是：每次只允许增加或删除 1 个节点。这背后的数学逻辑非常优雅：只要每次只变动 1 个节点，那么 $C\\\_{old}$ 的多数派和 $C\\\_{new}$ 的多数派必定存在交集。比如从 3 变 4，旧多数派是 2，新多数派是 3，你不可能在 4 个节点里凑出一个 2 人团体和一个 3 人团体且它们互不重叠。通过把“加两台机器”拆分成“加一台，等提交完毕，再加一台”，就从数学根源上扼杀了双主的可能。&#x20;
\&#x20;
\&#x20;
解法二：共同一致性（Joint Consensus）这是原版论文里提出的两阶段提交法。Leader 不会直接把集群从 $C\\\_{old}$ 切到 $C\\\_{new}$，而是先下发一个中间过渡状态，叫做 $C\\\_{old,new}$。当节点处于这个过渡状态时，任何日志的提交或选举，都必须同时满足 $C\\\_{old}$ 的多数派和 $C\\\_{new}$ 的多数派（也就是既要拿到 ABC 里的两票，也要拿到 ABCDE 里的三票）。这相当于用强制的"双重多数派"锁死了选举过程，等到这个过渡状态在集群中被多数派确认后，再安全地切换到单阶段的 $C\\\_{new}$。&#x20;
&#x20;
&#x20;
&#x20;
OK啊，这是我提供的raft算法的实现的基本信息，你可以阅读一下admin相关的源码然后告诉我改动的思路和措施，我觉得我提供的非常详细易懂了，有一两个地方可能需要你自行帮我补充，但是几乎所有的思路我应该都提供给你了，这个raft算法我真是解释的力竭了，哈哈

```
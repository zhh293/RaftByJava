# Redis 与 Raft 的取舍

> 学完 Raft 之后再来看 Redis 的设计，会发现一个很有意思的事情：Redis 明明知道 Raft 能提供强一致性，但它偏偏不用（至少不完整地用）。这不是因为 Redis 的工程师不行，恰恰相反，这是一个非常清醒的架构决策。本文从 Redis 主从、Sentinel、Cluster 三个层面，分析 Redis 对 Raft 思想的取舍——哪些借鉴了，哪些故意放弃了，为什么。

---

## 一、先明确一个前提：Redis 和 Raft 解决的问题不在一个层面

Raft 解决的核心问题是：**多个节点之间如何对一系列操作达成完全一致的共识。** 代价是每次写入都要等多数派确认，延迟不可避免。

Redis 解决的核心问题是：**如何用内存提供极致低延迟的数据访问。** 它是缓存，不是数据库。对 Redis 来说，"快"比"绝对不丢"重要得多。

所以 Redis 从设计哲学的根子上就做了一个 CAP 取舍：**在网络分区（P）不可避免的前提下，Redis 选择了可用性（A）而放弃了强一致性（C）。** 而 Raft 是典型的 CP 系统——宁可暂时不可用（选举期间拒绝写入），也要保证数据绝对一致。

这个根本性的分歧决定了 Redis 对 Raft 的态度：不是全盘接受，而是"选举的思想我借用一下，数据复制那套我不要"。

---

## 二、Redis 主从复制：彻底放弃 Raft 的数据同步

### Raft 的做法（回顾）

Raft 里写入流程是：Leader 收到写请求 → 写本地日志 → 广播给 Follower → 等多数派确认 → 提交 → 返回客户端成功。

这里最关键的是那个"等多数派确认"——客户端收到"成功"的时候，数据已经安全地存在于多数派节点上了。就算 Leader 立刻宕机，数据也不会丢。

### Redis 的做法：异步复制

Redis 的主从复制是这样的：Master 收到写请求 → 写入内存 → **立刻返回客户端成功** → 然后异步地把命令发给 Slave。

注意区别：Redis 不等任何 Slave 确认就直接返回了。Master 告诉客户端"写入成功"的那一刻，数据可能只存在于 Master 一台机器的内存里。如果这时候 Master 宕机了，数据就丢了——Slave 还没来得及收到这条命令。

### 为什么 Redis 要这么做

原因就一个字：**快**。

Raft 的多数派确认至少需要一次网络往返（几毫秒），还可能需要 Follower 落盘（又几毫秒）。对于一个追求微秒级响应的内存数据库来说，每次写入都多等几毫秒，性能直接打折几十倍。Redis 的典型场景是缓存、计数器、排行榜、session 存储——这些场景下，"偶尔丢一两条数据"的代价远远小于"每次写入都慢 10 倍"的代价。

Redis 提供了一个 `WAIT` 命令，可以让客户端手动等待 N 个 Slave 确认写入。但这本质上是把选择权交给了用户——绝大多数用户不会用它，因为用了就等于放弃了 Redis 的性能优势。

### 复制的具体机制：PSYNC

Redis 主从之间的数据同步有两种模式：

**全量复制**：Slave 第一次连接 Master，或者断线太久（复制缓冲区的数据已经被覆盖了），Master 生成一个 RDB 快照发给 Slave，然后把快照生成期间积累的新命令再补发过去。类似 Raft 里的 Snapshot + 增量日志追赶。

**增量复制（PSYNC）**：Slave 短暂断线后重新连接，通过 replication offset（复制偏移量）告诉 Master"我同步到了哪里"。如果 Master 的复制缓冲区（repl_backlog）里还有那个 offset 之后的数据，就只把增量部分发过来。类似 Raft 里 Follower 断线重连后 Leader 从 nextIndex 开始补发日志。

但有一个关键区别：Raft 的日志是持久化的，Follower 断线再久都能通过 Snapshot + 日志追赶恢复。Redis 的复制缓冲区是一个固定大小的环形缓冲区（默认 1MB），如果 Slave 断线太久，缓冲区被覆盖了，就只能全量复制。这反映了 Redis 对内存的极度珍惜——它不愿意为了"可能的断线恢复"而保留大量历史数据。

---

## 三、Redis Sentinel：借用 Raft 选举，但只用了一半

### Sentinel 是什么

Sentinel（哨兵）是 Redis 的高可用方案。它的职责很单一：监控 Master 是否存活，如果 Master 挂了，从 Slave 中选一个提升为新 Master。

注意 Sentinel **不参与数据的读写**，它只是一个"观察者"和"决策者"。数据复制还是走上面说的异步主从机制。

### Sentinel 选举中的 Raft 影子

当多个 Sentinel 同时检测到 Master 下线时，需要选出一个"领头 Sentinel"来执行故障转移操作。这个选举过程几乎就是一个简化版的 Raft Leader 选举：

**epoch ≈ term。** 每个 Sentinel 维护一个 `currentEpoch`，相当于 Raft 的 term。每次发起选举就 epoch++，跟 Raft 一模一样。

**一个 epoch 只投一票。** Sentinel 在同一个 epoch 里只会给一个候选者投票，先到先得。这跟 Raft 的"一个 term 只能 votedFor 一个 Candidate"完全一样。

**多数派当选。** 需要超过半数 Sentinel 投票才能成为领头 Sentinel，跟 Raft 需要多数派一样。

**随机延迟避免活锁。** 跟 Raft 的随机选举超时一样，Sentinel 也会在发起选举前加一个随机延迟，避免所有 Sentinel 同时发起选举导致选票瓜分。

### 但 Sentinel 没用 Raft 的哪些部分

**没有日志复制。** Raft 选出 Leader 后，Leader 要负责日志复制和多数派确认。Sentinel 选出领头后，领头只是执行一次性的故障转移操作（选一个 Slave 提升为 Master），不需要维护持续的日志同步。

**没有 prevLogIndex/prevLogTerm 校验。** Sentinel 之间不需要保证"操作历史一致"，它们只需要对"谁来执行这次故障转移"达成一致。

**没有日志新旧比较（第三步）。** Raft 投票时要比较 Candidate 的日志是否足够新。Sentinel 投票是纯粹的"先到先得"——第一个来要票的就给它，不看它有什么"资历"。

**没有心跳维持 Leader 地位。** Raft 的 Leader 通过持续的心跳来维持权威，心跳停了就会触发新选举。Sentinel 的领头 Sentinel 执行完故障转移就"退位"了，不存在持续的 Leader 角色。

所以本质上，Sentinel 借用的只是 Raft **选举**那一小块逻辑（epoch + 一票一投 + 多数派），用来解决"多个 Sentinel 谁来操刀"这个协调问题。Raft 真正复杂和有价值的部分——日志复制、一致性保证、安全性证明——Sentinel 完全不需要。

---

## 四、Redis Cluster：去中心化的另一条路

### 为什么 Redis Cluster 不用 Raft 做数据分片

上一篇《我眼里的 Multi-Raft》讲了 TiKV 的做法：用 Range 分片 + 每个分片一个 Raft Group。Redis Cluster 也做了数据分片（16384 个 Hash Slot），但它**没有**给每个分片配一个 Raft Group。为什么？

还是回到那个根本的设计哲学：**Redis 追求极致的简单和极致的快。**

如果每个 Slot 都跑一个 Raft Group，每次写入都要多数派确认，那 Redis Cluster 就变成了一个分布式强一致数据库——它就变成 TiKV 了。这与 Redis 的定位完全矛盾。Redis 要的是"写入快、偶尔丢数据可以接受"，不是"写入慢但绝对不丢"。

所以 Redis Cluster 的数据复制还是异步的——每个 Master 负责自己那些 Slot 的读写，异步地把数据同步给自己的 Slave。跟单机主从模式一模一样，只是现在有多个 Master 各管一段 Slot。

### Hash Slot 分片 vs Range 分片

TiKV 用 Range 分片（按 key 的范围切），Redis Cluster 用 Hash 分片（对 key 做 CRC16 然后 mod 16384）。

为什么选 Hash？因为 Redis 的使用场景里，范围查询（scan 一段连续的 key）不是主要需求。Redis 更多是点查——get 一个 key、set 一个 key。Hash 分片的好处是数据天然均匀分布，不会出现热点倾斜（某个 key 范围特别热导致对应节点负载过重）。而且 Hash 分片不需要一个中心化的"分片元数据服务"（像 TiKV 的 PD）来维护 key 范围到节点的映射——每个客户端自己算一下 hash 就知道该找哪个节点。

代价是什么？Hash 分片做不了高效的范围查询，而且扩容缩容时需要迁移 Slot（rehash），不如 Range 分片灵活。但对 Redis 的场景来说，这些代价可以接受。

### Gossip 协议：去中心化的集群管理

TiKV 有 PD 作为中心化的调度器来管理所有 Region 的元数据。Redis Cluster 没有这样的中心节点——所有节点通过 **Gossip 协议**互相通信，交换集群状态信息。

Gossip 的工作方式很简单：每个节点每隔一段时间随机选几个其他节点，互相交换自己知道的信息（谁负责哪些 Slot、谁挂了、谁是谁的 Slave 等）。信息像"流言"一样在集群中扩散，最终所有节点都会知道完整的集群状态。

跟 Raft 比：
- Raft 是**强一致**的——所有节点对日志序列的认知完全相同，任何时刻都一样。
- Gossip 是**最终一致**的——信息扩散需要时间，某一瞬间不同节点对集群状态的认知可能不一样，但最终会收敛。

为什么 Redis Cluster 选 Gossip 而不选 Raft？

第一，Gossip 没有 Leader，任何节点都是对等的。这意味着没有单点瓶颈——不像 Raft 只有 Leader 能处理写入，Gossip 里每个节点都可以独立服务自己负责的 Slot。

第二，Gossip 更耐网络分区。Raft 在网络分区时少数派完全不可用（凑不齐多数派）。Gossip 即使部分节点不可达，剩下的节点之间仍然可以交换信息，系统退化但不停摆。

第三，Gossip 实现简单，不需要维护复杂的日志状态机。对于"集群元数据同步"这种需求来说，最终一致就够了——Slot 分配信息不会频繁变化，晚几秒知道无所谓。

### Redis Cluster 的故障转移选举

当某个 Master 挂了，它的 Slave 需要被提升为新 Master。这个过程也用到了类似 Raft 的选举，但有自己的特色：

**投票者不同。** Raft 里所有节点都能投票。Redis Cluster 里**只有 Master 节点**能投票，Slave 不能。这是因为 Redis Cluster 的故障转移是"Slave 请求其他 Master 给自己授权升级"，Master 们作为权威来决定。

**epoch 的双层结构。** Redis Cluster 有两个 epoch：

- **currentEpoch**：整个集群的全局逻辑时钟，类似 Raft 的 term。每次有故障转移发生就递增。所有节点通过 Gossip 交换后最终收敛到同一个值。
- **configEpoch**：每个 Master 独有的配置版本号，标记"我是在哪次故障转移中成为这些 Slot 的负责人的"。当 Slot 归属发生争议时，configEpoch 大的那个节点赢。

这比 Raft 的单一 term 复杂，因为 Redis Cluster 是去中心化的——没有一个全局权威来仲裁，只能靠 epoch 的大小关系来解决冲突。

**选举流程：**

1. Slave 检测到自己的 Master 被标记为 FAIL（多数 Master 都同意它挂了）
2. Slave 递增 currentEpoch，向所有 Master 广播 `FAILOVER_AUTH_REQUEST`（"我要升级，请投票"）
3. 每个 Master 在同一个 currentEpoch 里只投一票，先到先得（跟 Raft/Sentinel 一样）
4. Slave 收集到超过半数 Master 的投票后，接管原 Master 的 Slot，成为新 Master
5. 新 Master 广播自己的 configEpoch（等于当前的 currentEpoch），告诉全集群"这些 Slot 现在归我管了"

**跟 Raft 选举的关键区别：没有日志新旧比较。** Raft 投票时会比较 Candidate 的日志是否足够新（lastLogTerm + lastLogIndex），确保选出的 Leader 拥有所有已提交的数据。Redis Cluster 的选举**不比较数据新旧**——Master 投票时不检查这个 Slave 的数据是不是最新的。

为什么不比较？因为 Redis 的主从复制本来就是异步的，数据丢失是"设计内允许"的事情。既然已经接受了"故障转移可能丢数据"这个前提，那比较日志新旧也没有太大意义——就算选了数据最新的 Slave，它也不保证有 Master 宕机前最后几条写入。

不过 Redis 4.0 之后引入了一个优化：如果一个 Master 下面有多个 Slave，数据偏移量（replication offset）更大的 Slave 会**优先发起选举**（延迟更短），这样大概率会选出数据相对最新的 Slave。但这只是一个"尽力而为"的优化，不是安全性保证。

---

## 五、三者对照总结

把 Raft、Redis Sentinel、Redis Cluster 放在一起比较，可以清楚地看出 Redis 的取舍哲学：

**数据复制方面：**
- Raft：同步复制，多数派确认后才算成功。**保证不丢。**
- Redis（不管是主从、Sentinel 还是 Cluster）：异步复制，Master 写完内存直接返回。**可能丢最后几条。**

**选举方面：**
- Raft：epoch（term）+ 一票一投 + 多数派 + **日志新旧比较**。保证选出的 Leader 拥有所有已提交数据。
- Sentinel：epoch + 一票一投 + 多数派。**不比较日志新旧**，只是协调"谁来操刀"。
- Cluster：currentEpoch + 一票一投 + 多数 Master 投票。**不比较日志新旧**，但偏移量大的 Slave 优先发起。

**集群管理方面：**
- Raft/Multi-Raft（TiKV）：中心化的 PD 调度 + 强一致的元数据。
- Redis Cluster：去中心化的 Gossip + 最终一致的元数据。

**分片方面：**
- Multi-Raft（TiKV）：Range 分片，每个分片一个 Raft Group。支持范围查询和动态 Split/Merge。
- Redis Cluster：Hash Slot 分片，每个分片只是普通的异步主从。支持点查，不支持高效范围查询。

---

## 六、设计哲学的对比：不同的世界观

### Raft 的世界观：宁可慢，不能错

Raft（以及 TiDB/etcd 这些系统）的根本信仰是：**数据一致性是不可妥协的底线。** 性能可以通过工程优化来提升（batch、pipeline、async apply），但一致性不能有任何折扣。宁可在选举期间拒绝服务几秒钟，也不允许出现两个 Leader 同时接受写入的情况。宁可每次写入多等几毫秒，也不允许已经返回成功的数据丢失。

这个世界观适合的场景是：金融交易、订单系统、配置中心、分布式锁——这些场景下数据丢失意味着真金白银的损失或者系统行为的不确定。

### Redis 的世界观：宁可丢，不能慢

Redis 的根本信仰是：**速度是第一优先级，可用性是第二优先级，一致性排最后。** 它假设的使用场景是：缓存可以重建（从数据库重新加载），计数器可以容忍小误差，session 丢了大不了让用户重新登录。在这些场景下，"每次写入多等 5 毫秒"对用户体验的伤害远远大于"百万次请求里偶尔丢一两条"的影响。

所以 Redis 不是"不会"实现 Raft 的完整流程，而是**故意不这么做**。它精准地从 Raft 里借了选举协调的思想（因为"谁来当 Master"这件事确实需要共识），但把数据同步这块换成了异步模式（因为数据一致性可以让步给速度）。

### 有没有折中方案？

有。Redis 提供了几个"半同步"的机制，让用户自己选择想要的一致性级别：

- **`WAIT` 命令**：客户端可以手动等待 N 个 Slave 确认。相当于临时把异步复制变成"准同步"，但代价是这次写入的延迟变高。
- **`min-replicas-to-write`** + **`min-replicas-max-lag`**：配置 Master 至少要有 N 个 Slave 在线且延迟不超过 M 秒，否则 Master 拒绝写入。这相当于给异步复制加了一个"底线"——不至于完全"裸奔"。

但这些都是可选的、非默认的。Redis 的默认行为永远是"写完就返回，Slave 爱追不追"。这就是它的设计哲学——把选择权给用户，但默认站在"快"这一边。

---

## 七、一些我自己的想法

**关于"Redis 不一致"这件事。** 学了 Raft 之后容易觉得 Redis 的异步复制"不够好"，但这其实是一种视角偏差。Redis 从来没承诺过强一致——它的定位就是缓存和辅助存储。拿缓存去跟数据库比一致性，就像拿自行车去跟汽车比载重一样，不公平也没意义。正确的做法是把 Redis 和强一致的数据库（如 TiDB、MySQL）配合使用——数据的权威来源是数据库，Redis 只是加速层。

**关于 epoch 和 term 本质上是一回事。** 不管叫什么名字，它们解决的都是同一个问题：怎么给分布式系统里的事件定一个全局顺序。term/epoch 递增保证了"新的决策覆盖旧的决策"，"一票一投"保证了"同一轮里不会有两个赢家"。这是分布式共识最核心的两个性质，不管哪个系统都逃不开。

**关于为什么 Redis Cluster 坚持去中心化。** 有了 PD 这种中心化调度器不是更简单吗？确实更简单，但中心化意味着 PD 本身也需要高可用方案（PD 自己也是 Raft 集群），引入了额外的组件和复杂度。Redis 的哲学是尽量少依赖外部组件，一个 redis-server 二进制就能搞定所有事情。去中心化虽然状态收敛慢一点，但运维简单——不需要额外部署和维护一个调度集群。对于 Redis 的场景来说，这个取舍是合理的。

**关于"什么时候该用哪个"的判断标准。** 说到底就一句话：你的数据丢了能不能重建？能重建（缓存、临时会话、热点数据）→ 用 Redis，享受极致性能。不能重建（交易记录、用户余额、系统配置）→ 用强一致的存储（TiDB、etcd），接受性能代价。两者不是互相替代的关系，而是互相配合的关系。
